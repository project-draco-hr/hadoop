{
  DummyHAService svc=idx == null ? null : svcs[idx];
  ActiveStandbyElectorTestUtil.waitForActiveLockData(ctx,zks,ZKFailoverController.ZK_PARENT_ZNODE_DEFAULT,(idx == null) ? null : Ints.toByteArray(svc.index));
}
