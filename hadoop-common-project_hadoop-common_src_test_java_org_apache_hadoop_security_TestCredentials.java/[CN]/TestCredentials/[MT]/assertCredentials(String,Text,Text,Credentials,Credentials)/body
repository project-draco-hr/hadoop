{
  assertEquals(tag + ": test token count",a.numberOfTokens(),b.numberOfTokens());
  assertEquals(tag + ": test service",a.getToken(alias).getService(),b.getToken(alias).getService());
  assertEquals(tag + ": test kind",a.getToken(alias).getKind(),b.getToken(alias).getKind());
  assertTrue(tag + ": test password",Arrays.equals(a.getToken(alias).getPassword(),b.getToken(alias).getPassword()));
  assertTrue(tag + ": test identifier",Arrays.equals(a.getToken(alias).getIdentifier(),b.getToken(alias).getIdentifier()));
  assertEquals(tag + ": test number of keys",a.numberOfSecretKeys(),b.numberOfSecretKeys());
  assertTrue(tag + ":test key values",Arrays.equals(a.getSecretKey(keykey),b.getSecretKey(keykey)));
}
