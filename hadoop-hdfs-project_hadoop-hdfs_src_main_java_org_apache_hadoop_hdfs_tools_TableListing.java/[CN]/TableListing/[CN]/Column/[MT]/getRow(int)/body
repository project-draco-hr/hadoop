{
  String raw=rows.get(i);
  int paddingLength=maxLength - raw.length();
  String padding=(paddingLength <= 0) ? "" : StringUtils.repeat(" ",paddingLength);
  if (justification == Justification.LEFT) {
    return raw + padding;
  }
 else {
    return padding + raw;
  }
}
