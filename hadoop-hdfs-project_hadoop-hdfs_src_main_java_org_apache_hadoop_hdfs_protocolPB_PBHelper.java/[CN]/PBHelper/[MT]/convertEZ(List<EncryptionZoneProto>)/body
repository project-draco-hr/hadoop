{
  final ArrayList<EncryptionZone> ret=Lists.newArrayListWithCapacity(ezs.size());
  for (  EncryptionZoneProto a : ezs) {
    final EncryptionZone ez=new EncryptionZone(a.getPath(),a.getKeyName());
    ret.add(ez);
  }
  return ret;
}
