{
switch (type) {
case DISK:
    return StorageTypeProto.DISK;
case SSD:
  return StorageTypeProto.SSD;
default :
Preconditions.checkState(false,"Failed to update StorageTypeProto with new StorageType " + type.toString());
return StorageTypeProto.DISK;
}
}
