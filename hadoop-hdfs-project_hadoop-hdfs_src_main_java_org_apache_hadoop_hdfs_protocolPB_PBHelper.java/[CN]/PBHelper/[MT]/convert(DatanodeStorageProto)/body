{
  if (s.hasStorageType()) {
    return new DatanodeStorage(s.getStorageID(),PBHelper.convertState(s.getState()),PBHelper.convertType(s.getStorageType()));
  }
 else {
    return new DatanodeStorage(s.getStorageID(),PBHelper.convertState(s.getState()));
  }
}
