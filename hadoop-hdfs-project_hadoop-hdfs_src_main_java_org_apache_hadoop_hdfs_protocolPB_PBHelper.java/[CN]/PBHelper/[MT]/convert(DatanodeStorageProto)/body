{
  if (s.hasStorageType()) {
    return new DatanodeStorage(s.getStorageUuid(),PBHelper.convertState(s.getState()),PBHelper.convertType(s.getStorageType()));
  }
 else {
    return new DatanodeStorage(s.getStorageUuid(),PBHelper.convertState(s.getState()));
  }
}
