{
  ReportNewAggregatorsInfoRequest request=Records.newRecord(ReportNewAggregatorsInfoRequest.class);
  request.setAppAggregatorsList(Arrays.asList(AppAggregatorsMap.newInstance(id,aggregatorAddr)));
  return request;
}
