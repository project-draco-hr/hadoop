{
  if (!renameSupported())   return;
  Path src=getTestRootPath("test/hadoop/NonExistingPath");
  Path dst=getTestRootPath("test/new/newpath");
  try {
    fc.rename(src,dst);
    Assert.assertTrue("rename of non existing path should have Assert.failed",false);
  }
 catch (  Exception e) {
  }
}
