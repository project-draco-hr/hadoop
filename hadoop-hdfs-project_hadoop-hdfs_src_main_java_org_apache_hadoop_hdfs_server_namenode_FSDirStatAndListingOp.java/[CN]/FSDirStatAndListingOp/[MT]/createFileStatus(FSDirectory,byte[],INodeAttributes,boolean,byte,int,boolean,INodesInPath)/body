{
  if (needLocation) {
    return createLocatedFileStatus(fsd,path,nodeAttrs,storagePolicy,snapshot,isRawPath,iip);
  }
 else {
    return createFileStatus(fsd,path,nodeAttrs,storagePolicy,snapshot,isRawPath,iip);
  }
}
