{
  fsWorkingPath=new Path(conf.get(YarnConfiguration.FS_NODE_LABELS_STORE_URI,YarnConfiguration.DEFAULT_FS_NODE_LABELS_STORE_URI));
  rootDirPath=new Path(fsWorkingPath,ROOT_DIR_NAME);
  setFileSystem(conf);
  fs.mkdirs(rootDirPath);
}
