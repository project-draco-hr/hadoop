{
  return new LocatedBlocks(lb.getFileLength(),lb.getUnderConstruction(),convertLocatedBlock(lb.getBlocksList()),lb.hasLastBlock() ? convert(lb.getLastBlock()) : null,lb.getIsLastBlockComplete(),lb.hasFileEncryptionInfo() ? convert(lb.getFileEncryptionInfo()) : null);
}
