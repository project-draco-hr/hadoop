{
  setFailoverConfigurations(conf,logicalName,Iterables.transform(nnAddresses,new Function<InetSocketAddress,String>(){
    @Override public String apply(    InetSocketAddress addr){
      return "hdfs://" + addr.getHostName() + ":"+ addr.getPort();
    }
  }
));
}
