{
  ConcurrentMap<ContainerId,Container> containers=getNMContext().getContainers();
  Assert.assertTrue(containers.isEmpty());
  super.rebootNodeStatusUpdater();
  try {
    syncBarrier.await();
  }
 catch (  InterruptedException e) {
  }
catch (  BrokenBarrierException e) {
  }
}
