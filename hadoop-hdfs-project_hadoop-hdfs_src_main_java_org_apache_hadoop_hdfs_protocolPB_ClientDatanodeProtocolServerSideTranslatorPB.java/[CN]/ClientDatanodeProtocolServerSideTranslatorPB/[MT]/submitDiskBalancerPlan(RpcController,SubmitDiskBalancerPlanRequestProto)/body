{
  try {
    impl.submitDiskBalancerPlan(request.getPlanID(),request.hasPlanVersion() ? request.getPlanVersion() : 0,request.hasMaxDiskBandwidth() ? request.getMaxDiskBandwidth() : 0,request.getPlan());
    SubmitDiskBalancerPlanResponseProto response=SubmitDiskBalancerPlanResponseProto.newBuilder().build();
    return response;
  }
 catch (  Exception e) {
    throw new ServiceException(e);
  }
}
