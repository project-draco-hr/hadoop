{
  ListReconfigurablePropertiesResponseProto.Builder builder=ListReconfigurablePropertiesResponseProto.newBuilder();
  try {
    for (    String name : impl.listReconfigurableProperties()) {
      builder.addName(name);
    }
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
  return builder.build();
}
