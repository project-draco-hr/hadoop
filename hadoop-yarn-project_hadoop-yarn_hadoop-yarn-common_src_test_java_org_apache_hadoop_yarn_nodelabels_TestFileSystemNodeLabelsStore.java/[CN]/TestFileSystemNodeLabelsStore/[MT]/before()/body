{
  mgr=new MockNodeLabelManager();
  conf=new Configuration();
  File tempDir=File.createTempFile("nlb",".tmp");
  tempDir.delete();
  tempDir.mkdirs();
  tempDir.deleteOnExit();
  conf.set(YarnConfiguration.FS_NODE_LABELS_STORE_URI,tempDir.getAbsolutePath());
  mgr.init(conf);
  mgr.start();
}
