def handleIdleJobTracker(self):
    self.log.critical(('Detected idle job tracker for %s seconds. The allocation will be cleaned up.' % self.cfg['ringmaster']['idleness-limit']))
    self.__idlenessDetected = True
