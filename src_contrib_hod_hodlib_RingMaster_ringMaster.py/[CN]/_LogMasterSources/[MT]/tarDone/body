def tarDone(self, uri):
    str = ('%s is done' % uri)
    self.log.debug(str)
    lock = self.tarSourceLock
    lock.acquire()
    c = self.count[uri]
    self.count[uri] = (c - 1)
    lock.release()
    return uri
