{
  if (compressedDirectBuf.remaining() > 0) {
    return false;
  }
  if (uncompressedDirectBufLen <= 0) {
    if (userBufLen <= 0) {
      return true;
    }
 else {
      setInputFromSavedData();
    }
  }
  return false;
}
