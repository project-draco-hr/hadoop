{
  this.pipelineFactory=new ChannelPipelineFactory(){
    @Override public ChannelPipeline getPipeline(){
      return Channels.pipeline(new RpcFrameDecoder(),new SimpleTcpClientHandler(request));
    }
  }
;
  return this.pipelineFactory;
}
