{
  byte[][] erased=ECChunk.toArray(erasedChunks);
  byte[][] recovered=ECChunk.toArray(recoveredChunks);
  for (int i=0; i < erasedChunks.length; ++i) {
    assertArrayEquals("Decoding and comparing failed.",erased[i],recovered[i]);
  }
}
