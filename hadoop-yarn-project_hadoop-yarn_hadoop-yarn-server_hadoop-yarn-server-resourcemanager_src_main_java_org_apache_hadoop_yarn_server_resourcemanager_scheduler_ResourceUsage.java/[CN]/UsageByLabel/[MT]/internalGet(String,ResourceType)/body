{
  try {
    readLock.lock();
    UsageByLabel usage=usages.get(label);
    if (null == usage) {
      return Resources.none();
    }
    return normalize(usage.get(type));
  }
  finally {
    readLock.unlock();
  }
}
