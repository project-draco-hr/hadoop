{
  return ComparisonChain.start().compare(scope,other.scope,Ordering.explicit(ACCESS,DEFAULT)).compare(type,other.type,Ordering.explicit(USER,GROUP,MASK,OTHER)).compare(name,other.name,Ordering.natural().nullsFirst()).result();
}
