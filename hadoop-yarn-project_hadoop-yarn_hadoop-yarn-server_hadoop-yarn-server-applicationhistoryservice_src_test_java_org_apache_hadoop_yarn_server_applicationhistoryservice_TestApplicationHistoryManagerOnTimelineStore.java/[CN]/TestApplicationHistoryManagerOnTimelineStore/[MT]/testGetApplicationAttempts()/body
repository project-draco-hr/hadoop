{
  Collection<ApplicationAttemptReport> appAttempts=historyManager.getApplicationAttempts(ApplicationId.newInstance(0,1)).values();
  Assert.assertNotNull(appAttempts);
  Assert.assertEquals(SCALE,appAttempts.size());
}
