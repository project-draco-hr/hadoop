{
  Collection<ContainerReport> containers=historyManager.getContainers(ApplicationAttemptId.newInstance(ApplicationId.newInstance(0,1),1)).values();
  Assert.assertNotNull(containers);
  Assert.assertEquals(SCALE,containers.size());
}
