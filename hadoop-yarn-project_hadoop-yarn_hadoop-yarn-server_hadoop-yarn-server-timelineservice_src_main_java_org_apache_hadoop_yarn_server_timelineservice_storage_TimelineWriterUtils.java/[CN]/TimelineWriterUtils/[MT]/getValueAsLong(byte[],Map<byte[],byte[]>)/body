{
  if (values == null) {
    return 0;
  }
  byte[] value=values.get(key);
  if (value != null) {
    Number val=(Number)GenericObjectMapper.read(value);
    return val.longValue();
  }
 else {
    return 0L;
  }
}
