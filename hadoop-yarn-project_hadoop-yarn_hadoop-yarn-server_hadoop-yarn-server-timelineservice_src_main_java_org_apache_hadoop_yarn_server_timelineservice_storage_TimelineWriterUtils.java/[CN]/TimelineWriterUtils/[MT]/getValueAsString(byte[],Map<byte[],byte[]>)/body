{
  if (values == null) {
    return EMPTY_STRING;
  }
  byte[] value=values.get(key);
  if (value != null) {
    return GenericObjectMapper.read(value).toString();
  }
 else {
    return EMPTY_STRING;
  }
}
