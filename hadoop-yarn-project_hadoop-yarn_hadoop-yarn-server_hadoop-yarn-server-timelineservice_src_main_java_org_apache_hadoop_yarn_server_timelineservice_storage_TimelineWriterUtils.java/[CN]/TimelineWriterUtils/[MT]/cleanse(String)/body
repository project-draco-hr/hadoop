{
  if (token == null || token.length() == 0) {
    return token;
  }
  String cleansed=token.replaceAll(SPACE,UNDERSCORE);
  cleansed=cleansed.replaceAll(TimelineEntitySchemaConstants.ROW_KEY_SEPARATOR,UNDERSCORE);
  return cleansed;
}
