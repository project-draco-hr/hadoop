{
  this.role=role;
  this.haEnabled=DFSUtil.isHAEnabled(conf);
  this.state=!haEnabled ? ACTIVE_STATE : STANDBY_STATE;
  try {
    initialize(conf);
  }
 catch (  IOException e) {
    this.stop();
    throw e;
  }
}
