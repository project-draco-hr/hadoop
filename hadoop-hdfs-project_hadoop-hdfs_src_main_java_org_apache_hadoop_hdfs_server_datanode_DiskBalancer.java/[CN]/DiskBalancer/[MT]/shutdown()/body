{
  lock.lock();
  try {
    this.isDiskBalancerEnabled=false;
    if ((this.future != null) && (!this.future.isDone())) {
      this.blockMover.setExitFlag();
      shutdownExecutor();
    }
  }
  finally {
    lock.unlock();
  }
}
