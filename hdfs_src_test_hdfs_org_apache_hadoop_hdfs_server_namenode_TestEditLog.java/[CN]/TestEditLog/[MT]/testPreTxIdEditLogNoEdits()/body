{
  FSNamesystem namesys=Mockito.mock(FSNamesystem.class);
  namesys.dir=Mockito.mock(FSDirectory.class);
  int numEdits=testLoad(StringUtils.hexStringToByte("ffffffed"),namesys);
  assertEquals(0,numEdits);
}
