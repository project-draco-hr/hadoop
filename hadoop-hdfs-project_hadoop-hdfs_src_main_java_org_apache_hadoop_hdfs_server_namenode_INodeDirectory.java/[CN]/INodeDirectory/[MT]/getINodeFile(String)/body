{
  final INode inode=getNode(path,false);
  if (inode == null) {
    throw new FileNotFoundException("File \"" + path + "\" not found");
  }
  if (!(inode instanceof INodeFile)) {
    throw new FileNotFoundException("Path \"" + path + "\" is not a file");
  }
  return (INodeFile)inode;
}
