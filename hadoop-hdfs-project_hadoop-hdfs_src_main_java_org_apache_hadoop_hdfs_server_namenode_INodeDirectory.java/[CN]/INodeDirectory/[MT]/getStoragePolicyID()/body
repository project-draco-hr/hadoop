{
  byte id=getLocalStoragePolicyID();
  if (id != BlockStoragePolicySuite.ID_UNSPECIFIED) {
    return id;
  }
  return getParent() != null ? getParent().getStoragePolicyID() : BlockStoragePolicySuite.ID_UNSPECIFIED;
}
