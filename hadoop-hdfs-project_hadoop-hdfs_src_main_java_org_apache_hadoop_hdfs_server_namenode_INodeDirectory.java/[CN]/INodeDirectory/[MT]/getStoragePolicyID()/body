{
  byte id=getLocalStoragePolicyID();
  if (id != BlockStoragePolicy.ID_UNSPECIFIED) {
    return id;
  }
  return getParent() != null ? getParent().getStoragePolicyID() : BlockStoragePolicy.ID_UNSPECIFIED;
}
