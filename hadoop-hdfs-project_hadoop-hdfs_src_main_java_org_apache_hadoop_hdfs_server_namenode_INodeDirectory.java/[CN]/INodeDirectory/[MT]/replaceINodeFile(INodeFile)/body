{
  assertChildrenNonNull();
  Preconditions.checkArgument(!(child instanceof INodeFileWithLink),"Child file is already an INodeFileWithLink, child=" + child);
  final INodeFileWithLink newChild=new INodeFileWithLink(child);
  final int i=searchChildrenForExistingINode(newChild);
  children.set(i,newChild);
  return newChild;
}
