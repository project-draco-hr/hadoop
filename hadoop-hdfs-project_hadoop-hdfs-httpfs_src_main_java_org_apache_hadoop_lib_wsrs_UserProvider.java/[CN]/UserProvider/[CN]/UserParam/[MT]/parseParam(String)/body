{
  if (str != null) {
    int len=str.length();
    if (len < 1 || len > 31) {
      throw new IllegalArgumentException(MessageFormat.format("Parameter [{0}], invalid value [{1}], it's length must be between 1 and 31",getName(),str));
    }
  }
  return super.parseParam(str);
}
