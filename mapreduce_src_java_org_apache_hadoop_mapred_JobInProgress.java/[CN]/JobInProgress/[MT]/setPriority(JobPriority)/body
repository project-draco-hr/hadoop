{
  if (priority == null) {
    priority=JobPriority.NORMAL;
  }
synchronized (this) {
    this.priority=priority;
    status.setJobPriority(priority);
    JobPriorityChangeEvent prEvent=new JobPriorityChangeEvent(jobId,priority);
    jobHistory.logEvent(prEvent,jobId);
  }
}
