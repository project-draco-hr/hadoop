{
  request.setNumContainers(request.getNumContainers() - 1);
  ask.remove(request);
  ask.add(org.apache.hadoop.yarn.util.BuilderUtils.newResourceRequest(request));
  LOG.info("DEBUG --- updateResourceRequest:" + " application=" + applicationId + " request="+ request);
}
