{
  long vmemBytes=container.getLaunchContext().getResource().getMemory() * 1024 * 1024L;
  container.dispatcher.getEventHandler().handle(new ContainerStartMonitoringEvent(container.getContainerID(),vmemBytes,-1));
  container.metrics.runningContainer();
}
