{
  try {
    cluster=new MiniDFSCluster.Builder(conf).numDataNodes(0).format(false).clusterId(clusterId).startupOption(operation).build();
    throw new AssertionError("NameNode should have failed to start");
  }
 catch (  Exception expected) {
    expected=null;
  }
}
