{
  while ((getBlockPoolSetSize() < datanode.getAllBpOs().length) || (getBlockPoolSetSize() < 1)) {
    try {
      Thread.sleep(5000);
    }
 catch (    InterruptedException e) {
      blockScannerThread.interrupt();
      return;
    }
  }
}
