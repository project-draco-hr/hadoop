{
  byte[] b=new byte[1];
  if (curPos < totalSize) {
    if (positionInVal < val.getLength()) {
      b[0]=val.getBytes()[positionInVal++];
      ++curPos;
    }
 else {
      int num=read(b);
      if (num < 0) {
        return num;
      }
    }
  }
 else {
    return -1;
  }
  return b[0];
}
