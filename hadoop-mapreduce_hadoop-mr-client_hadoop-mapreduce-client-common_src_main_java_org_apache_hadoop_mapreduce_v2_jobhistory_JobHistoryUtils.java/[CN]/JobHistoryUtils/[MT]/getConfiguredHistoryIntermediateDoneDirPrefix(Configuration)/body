{
  String doneDirPrefix=conf.get(JHConfig.HISTORY_INTERMEDIATE_DONE_DIR_KEY);
  if (doneDirPrefix == null) {
    doneDirPrefix=conf.get(MRConstants.APPS_STAGING_DIR_KEY) + "/history/done_intermediate";
  }
  return doneDirPrefix;
}
