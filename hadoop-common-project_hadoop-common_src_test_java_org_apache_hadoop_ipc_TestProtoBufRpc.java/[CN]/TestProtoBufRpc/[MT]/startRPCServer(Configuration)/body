{
  RPC.setProtocolEngine(conf,BlockingService.class,ProtobufRpcEngine.class);
  ServerImpl serverImpl=new ServerImpl();
  BlockingService service=TestProtobufRpcProto.newReflectiveBlockingService(serverImpl);
  RPC.Server server=RPC.getServer(BlockingService.class,service,ADDRESS,PORT,conf);
  server.start();
  return server;
}
