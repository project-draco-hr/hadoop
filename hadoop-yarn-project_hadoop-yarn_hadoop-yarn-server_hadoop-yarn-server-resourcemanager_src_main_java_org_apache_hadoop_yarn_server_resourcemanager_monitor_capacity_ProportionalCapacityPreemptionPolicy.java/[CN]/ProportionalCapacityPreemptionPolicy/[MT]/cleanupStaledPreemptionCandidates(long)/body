{
  for (Iterator<RMContainer> i=preemptionCandidates.keySet().iterator(); i.hasNext(); ) {
    RMContainer id=i.next();
    if (preemptionCandidates.get(id) + 2 * maxWaitTime < currentTime) {
      i.remove();
    }
  }
}
