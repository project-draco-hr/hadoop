{
  return Proxy.newProxyInstance(protocol.getClassLoader(),new Class[]{protocol},new InvocationHandler(){
    public Object invoke(    Object proxy,    Method method,    Object[] args) throws Throwable {
      return new Invoker(ReflectData.get().getProtocol(protocol),new ClientTransceiver(addr,ticket,conf,factory)).invoke(proxy,method,args);
    }
  }
);
}
