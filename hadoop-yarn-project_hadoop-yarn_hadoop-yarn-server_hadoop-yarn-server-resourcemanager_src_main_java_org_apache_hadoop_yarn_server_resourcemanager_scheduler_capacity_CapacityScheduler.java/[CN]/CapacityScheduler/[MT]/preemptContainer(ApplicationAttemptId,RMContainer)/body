{
  if (LOG.isDebugEnabled()) {
    LOG.debug("PREEMPT_CONTAINER: application:" + aid.toString() + " container: "+ cont.toString());
  }
  FiCaSchedulerApp app=appAttempts.get(aid);
  if (app != null) {
    app.addPreemptContainer(cont.getContainerId());
  }
}
