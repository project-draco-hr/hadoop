{
  int sum=0;
  for (  String x : fileBaseNames) {
    sum+=1 + ((x == null) ? 0 : x.length());
  }
  sum+=2 + fileDir.getCanonicalPath().length() + linkDir.getCanonicalPath().length();
  sum+=(CMD_EXE + " /q /c for %f in ( ) do " + Shell.WINUTILS+ " hardlink create \\%f %f").length();
  return sum;
}
