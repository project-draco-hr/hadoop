{
  Class<?> clazz=conf.getClass("fs.AbstractFileSystem." + uri.getScheme() + ".impl",null);
  if (clazz == null) {
    throw new IOException("No AbstractFileSystem for scheme: " + uri.getScheme());
  }
  return (AbstractFileSystem)newInstance(clazz,uri,conf);
}
