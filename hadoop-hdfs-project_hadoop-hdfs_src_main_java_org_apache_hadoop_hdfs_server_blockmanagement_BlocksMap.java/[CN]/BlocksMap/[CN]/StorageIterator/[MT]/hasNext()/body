{
  if (blockInfo == null) {
    return false;
  }
  while (nextIdx < blockInfo.getCapacity() && blockInfo.getDatanode(nextIdx) == null) {
    nextIdx++;
  }
  return nextIdx < blockInfo.getCapacity();
}
