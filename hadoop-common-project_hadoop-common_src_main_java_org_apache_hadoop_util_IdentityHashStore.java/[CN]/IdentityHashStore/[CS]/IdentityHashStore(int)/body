{
  Preconditions.checkArgument(capacity >= 0);
  if (capacity == 0) {
    this.capacity=0;
    this.buffer=null;
  }
 else {
    realloc((int)Math.pow(2,Math.ceil(Math.log(capacity) / Math.log(2))));
  }
}
