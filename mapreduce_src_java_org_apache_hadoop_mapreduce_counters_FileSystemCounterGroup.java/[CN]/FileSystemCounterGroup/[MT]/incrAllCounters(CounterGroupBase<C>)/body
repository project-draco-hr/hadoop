{
  if (checkNotNull(other,"other group") instanceof FileSystemCounterGroup<?>) {
    for (    Counter counter : other) {
      FSCounter c=(FSCounter)counter;
      findCounter(c.scheme,c.key).increment(counter.getValue());
    }
  }
}
