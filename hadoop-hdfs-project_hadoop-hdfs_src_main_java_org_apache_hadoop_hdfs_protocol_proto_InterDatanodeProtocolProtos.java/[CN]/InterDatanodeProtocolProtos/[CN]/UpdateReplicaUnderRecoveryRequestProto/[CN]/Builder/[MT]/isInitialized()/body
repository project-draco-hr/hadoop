{
  if (!hasBlock()) {
    return false;
  }
  if (!hasRecoveryId()) {
    return false;
  }
  if (!hasNewLength()) {
    return false;
  }
  if (!getBlock().isInitialized()) {
    return false;
  }
  return true;
}
