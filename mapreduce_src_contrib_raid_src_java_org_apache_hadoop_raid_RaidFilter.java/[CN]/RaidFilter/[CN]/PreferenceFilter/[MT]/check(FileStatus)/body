{
  Object firstChoicePPair=RaidNode.getParityFile(firstChoicePrefix,f.getPath(),getConf());
  if (firstChoicePPair == null) {
    return secondChoiceFilter.check(f);
  }
 else {
    return false;
  }
}
