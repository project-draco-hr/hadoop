{
  txid++;
  TransactionId id=myTransactionId.get();
  id.txid=txid;
  long end=now();
  numTransactions++;
  totalTimeTransactions+=(end - start);
  if (metrics != null)   metrics.addTransaction(end - start);
}
