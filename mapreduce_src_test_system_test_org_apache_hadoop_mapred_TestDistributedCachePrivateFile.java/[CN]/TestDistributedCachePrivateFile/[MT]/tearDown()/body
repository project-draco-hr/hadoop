{
  cluster.tearDown();
  dfs.delete(URIPATH,true);
  Collection<TTClient> tts=cluster.getTTClients();
  for (  TTClient tt : tts) {
    tt.kill();
  }
  for (  TTClient tt : tts) {
    tt.start();
  }
}
