{
  if (queueLabels != null && queueLabels.contains(RMNodeLabelsManager.ANY)) {
    return true;
  }
  if (nodeLabels == null || nodeLabels.isEmpty()) {
    return true;
  }
  if (queueLabels != null && Sets.intersection(queueLabels,nodeLabels).size() > 0) {
    return true;
  }
  return false;
}
