{
  AccessControlList acl;
  acl=new AccessControlList("*");
  assertTrue(acl.allAllowed());
  acl=new AccessControlList("  * ");
  assertTrue(acl.allAllowed());
  acl=new AccessControlList(" *");
  assertTrue(acl.allAllowed());
  acl=new AccessControlList("*  ");
  assertTrue(acl.allAllowed());
}
