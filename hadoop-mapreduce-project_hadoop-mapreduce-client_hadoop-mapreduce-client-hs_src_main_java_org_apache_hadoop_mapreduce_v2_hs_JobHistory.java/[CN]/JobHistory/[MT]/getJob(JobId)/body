{
  if (LOG.isDebugEnabled()) {
    LOG.debug("Looking for Job " + jobId);
  }
  Job job=null;
  try {
    job=findJob(jobId);
  }
 catch (  IOException e) {
    throw new YarnException(e);
  }
  return job;
}
