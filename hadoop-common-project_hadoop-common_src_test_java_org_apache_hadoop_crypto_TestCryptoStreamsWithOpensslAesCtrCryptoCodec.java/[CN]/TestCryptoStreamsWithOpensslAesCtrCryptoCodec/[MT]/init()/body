{
  Configuration conf=new Configuration();
  conf.set(HADOOP_SECURITY_CRYPTO_CODEC_CLASS_KEY,OpensslAesCtrCryptoCodec.class.getName() + "," + JceAesCtrCryptoCodec.class.getName());
  codec=CryptoCodec.getInstance(conf);
}
