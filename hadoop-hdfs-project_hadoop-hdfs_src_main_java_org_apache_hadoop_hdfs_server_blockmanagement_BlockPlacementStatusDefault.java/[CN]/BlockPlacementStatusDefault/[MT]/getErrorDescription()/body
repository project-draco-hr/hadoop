{
  if (isPlacementPolicySatisfied()) {
    return null;
  }
  return "Block should be additionally replicated on " + (requiredRacks - currentRacks) + " more rack(s). Total number of racks in the cluster: "+ totalRacks;
}
