{
  elector.joinElection(data);
  elector.processResult(Code.APIERROR.intValue(),zkLockPathName,null,zkLockPathName);
  Mockito.verify(mockApp,Mockito.times(1)).notifyFatalError("Received create error from Zookeeper. code:APIERROR");
}
