{
  if (dataState != DataState.DUMPED) {
synchronized (this) {
      if (dataState != DataState.DUMPED) {
        Preconditions.checkState(data != null);
        return data;
      }
    }
  }
  this.loadData();
  return data;
}
