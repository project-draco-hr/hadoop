{
  if (idx == -1) {
    makeRequest();
  }
 else   if ((entries != null) && (idx >= entries.size())) {
    if (!entries.hasMore()) {
      entries=null;
    }
 else {
      makeRequest();
    }
  }
}
