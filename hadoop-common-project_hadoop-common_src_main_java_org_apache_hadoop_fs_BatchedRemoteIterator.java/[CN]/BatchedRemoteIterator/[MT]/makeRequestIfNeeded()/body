{
  if (idx == -1) {
    makeRequest();
  }
 else   if ((entries != null) && (idx >= entries.size())) {
    if (entries.size() < maxRepliesPerRequest) {
      entries=null;
    }
 else {
      makeRequest();
    }
  }
}
