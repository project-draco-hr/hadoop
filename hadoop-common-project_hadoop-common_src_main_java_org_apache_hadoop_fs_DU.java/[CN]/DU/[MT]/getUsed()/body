{
  if (refreshUsed == null) {
    run();
  }
 else {
synchronized (DU.this) {
      if (duException != null) {
        IOException tmp=duException;
        duException=null;
        throw tmp;
      }
    }
  }
  return Math.max(used.longValue(),0L);
}
