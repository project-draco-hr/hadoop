{
  if (refreshUsed == null) {
    run();
  }
 else {
synchronized (DU.this) {
      if (duException != null) {
        IOException tmp=duException;
        duException=null;
        throw tmp;
      }
    }
  }
  return used.longValue();
}
