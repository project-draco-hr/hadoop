{
  if (requestedVCores >= scheduler.getAllocationConfiguration().getMaxResources(getName()).getVirtualCores()) {
    return true;
  }
  if (getParent() == null) {
    return false;
  }
  return getParent().isVCoresOverMaxResource(requestedVCores);
}
