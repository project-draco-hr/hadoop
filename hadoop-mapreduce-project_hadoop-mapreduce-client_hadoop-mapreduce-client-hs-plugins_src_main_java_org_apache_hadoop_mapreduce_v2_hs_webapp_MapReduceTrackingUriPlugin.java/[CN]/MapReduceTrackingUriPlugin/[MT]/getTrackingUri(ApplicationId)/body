{
  String jobSuffix=id.toString().replaceFirst("^application_","job_");
  String historyServerAddress=this.getConf().get(JHAdminConfig.MR_HISTORY_WEBAPP_ADDRESS);
  return new URI("http://" + historyServerAddress + "/jobhistory/job/"+ jobSuffix);
}
