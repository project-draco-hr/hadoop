{
  if (obj == this) {
    return true;
  }
  if (obj instanceof AccessKey) {
    AccessKey that=(AccessKey)obj;
    return this.keyID == that.keyID && isEqual(this.key,that.key) && this.expiryDate == that.expiryDate;
  }
  return false;
}
