{
  String appIdString=parseApplicationId(appIdToParse);
  final TimelineAggregatorsCollection aggregatorCollection=(TimelineAggregatorsCollection)context.getAttribute(TimelineAggregatorsCollection.AGGREGATOR_COLLECTION_ATTR_KEY);
  return aggregatorCollection.get(appIdString);
}
