{
  Option report=OptionBuilder.withLongOpt(REPORT).withDescription("List nodes that will benefit from running " + "DiskBalancer.").create();
  getReportOptions().addOption(report);
  opt.addOption(report);
  Option top=new Option(TOP,true,"specify the number of nodes to be listed which has data imbalance.");
  getReportOptions().addOption(top);
  opt.addOption(top);
  Option node=new Option(NODE,true,"Datanode address, it can be DataNodeID, IP or hostname.");
  getReportOptions().addOption(node);
  opt.addOption(node);
}
