{
switch (n.getType()) {
case FILE:
    return loadINodeFile(n);
case DIRECTORY:
  return loadINodeDirectory(n,parent.getStringTable());
case SYMLINK:
return loadINodeSymlink(n);
default :
break;
}
return null;
}
