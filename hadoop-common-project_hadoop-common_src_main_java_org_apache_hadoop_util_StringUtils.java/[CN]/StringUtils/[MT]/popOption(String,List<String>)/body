{
  for (Iterator<String> iter=args.iterator(); iter.hasNext(); ) {
    String cur=iter.next();
    if (cur.equals("--")) {
      break;
    }
 else     if (cur.equals(name)) {
      iter.remove();
      return true;
    }
  }
  return false;
}
