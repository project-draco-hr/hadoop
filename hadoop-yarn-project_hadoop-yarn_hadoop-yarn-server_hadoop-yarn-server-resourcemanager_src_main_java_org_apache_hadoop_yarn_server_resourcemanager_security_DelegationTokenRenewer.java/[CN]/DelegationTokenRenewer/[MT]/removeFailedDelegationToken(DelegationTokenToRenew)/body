{
  ApplicationId applicationId=t.applicationId;
  LOG.error("removing failed delegation token for appid=" + applicationId + ";t="+ t.token.getService());
  appTokens.get(applicationId).remove(t);
  allTokens.remove(t.token);
  if (t.timerTask != null) {
    t.timerTask.cancel();
  }
}
