{
  Signer signer=new Signer("secret".getBytes());
  try {
    signer.verifyAndExtract("test");
    Assert.fail();
  }
 catch (  SignerException ex) {
  }
catch (  Throwable ex) {
    Assert.fail();
  }
}
