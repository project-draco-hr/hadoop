{
  int index=file.lastIndexOf(".");
  int id=-1;
  if (index > 0) {
    try {
      id=Integer.parseInt(file.substring(index + 1));
    }
 catch (    NumberFormatException ex) {
    }
  }
  return id;
}
