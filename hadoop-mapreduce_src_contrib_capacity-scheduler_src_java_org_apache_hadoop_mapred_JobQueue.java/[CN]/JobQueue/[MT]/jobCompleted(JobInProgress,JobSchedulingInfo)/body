{
  LOG.info("Job " + job.getJobID().toString() + " submitted to queue "+ job.getProfile().getQueueName()+ " has completed");
  removeRunningJob(oldInfo);
  removeWaitingJob(oldInfo);
  jobCompleted(job);
}
