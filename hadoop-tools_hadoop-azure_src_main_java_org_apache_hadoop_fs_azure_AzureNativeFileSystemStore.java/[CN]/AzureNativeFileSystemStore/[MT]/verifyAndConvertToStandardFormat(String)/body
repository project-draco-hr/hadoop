{
  URI asUri=new URI(rawDir);
  if (asUri.getAuthority() == null || asUri.getAuthority().toLowerCase(Locale.US).equalsIgnoreCase(sessionUri.getAuthority().toLowerCase(Locale.US))) {
    return trim(asUri.getPath(),"/");
  }
 else {
    return null;
  }
}
