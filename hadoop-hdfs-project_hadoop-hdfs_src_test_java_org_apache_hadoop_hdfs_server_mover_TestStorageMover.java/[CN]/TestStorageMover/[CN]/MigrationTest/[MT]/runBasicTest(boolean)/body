{
  setupCluster();
  try {
    prepareNamespace();
    verify(true);
    setStoragePolicy();
    migrate();
    verify(true);
  }
  finally {
    if (shotdown) {
      shutdownCluster();
    }
  }
}
