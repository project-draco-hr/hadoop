{
  this.result=result;
  this.planID=planID;
  this.planFile=planFile;
  ObjectMapper mapper=new ObjectMapper();
  this.currentState=mapper.readValue(currentState,defaultInstance().constructCollectionType(List.class,DiskBalancerWorkEntry.class));
}
