{
  final List<Block> toInvalidate=pollNumBlocks(storageId,datanodeManager.blockInvalidateLimit);
  if (toInvalidate != null) {
    dn.addBlocksToBeInvalidated(toInvalidate);
  }
  return toInvalidate;
}
