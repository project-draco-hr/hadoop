{
  for (  EditLogFile log : logs) {
    if (!log.isCorrupt() && !log.isInProgress()) {
      return log;
    }
  }
  for (  EditLogFile log : logs) {
    if (!log.isCorrupt()) {
      return log;
    }
  }
  throw new IllegalStateException("No non-corrupt logs for txid " + startTxId);
}
