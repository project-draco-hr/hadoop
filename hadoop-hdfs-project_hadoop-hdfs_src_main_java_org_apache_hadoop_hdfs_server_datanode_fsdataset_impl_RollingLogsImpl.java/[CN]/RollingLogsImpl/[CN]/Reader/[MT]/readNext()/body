{
  line=null;
  try {
    if (reader != null && (line=reader.readLine()) != null) {
      return;
    }
    if (openFile()) {
      readNext();
    }
  }
  finally {
    if (!hasNext()) {
      close();
    }
  }
}
