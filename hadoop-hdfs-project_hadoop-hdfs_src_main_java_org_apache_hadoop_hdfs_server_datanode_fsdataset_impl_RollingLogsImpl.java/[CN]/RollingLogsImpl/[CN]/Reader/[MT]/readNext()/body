{
  line=null;
  try {
    if (reader != null && (line=reader.readLine()) != null) {
      return;
    }
    if (line == null) {
      if (openFile()) {
        readNext();
      }
    }
  }
  finally {
    if (!hasNext()) {
      close();
    }
  }
}
