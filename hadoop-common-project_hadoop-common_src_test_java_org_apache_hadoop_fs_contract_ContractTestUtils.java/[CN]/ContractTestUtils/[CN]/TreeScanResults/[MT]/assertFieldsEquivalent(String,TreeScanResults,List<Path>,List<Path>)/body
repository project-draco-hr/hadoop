{
  assertFalse("Duplicate  " + files + " in "+ this,containsDuplicates(ours));
  assertFalse("Duplicate  " + files + " in other "+ that,containsDuplicates(theirs));
  assertTrue(fieldname + " mismatch: between {" + this+ "}"+ " and {"+ that+ "}",collectionsEquivalent(files,that.files));
}
