{
  String name=getPath(item).getName();
  if (!caseSensitive) {
    name=name.toLowerCase(Locale.ENGLISH);
  }
  if (globPattern.matches(name)) {
    return Result.PASS;
  }
 else {
    return Result.FAIL;
  }
}
