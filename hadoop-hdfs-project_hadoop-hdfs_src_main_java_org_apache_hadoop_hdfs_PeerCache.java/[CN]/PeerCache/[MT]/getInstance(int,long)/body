{
  if (instance == null) {
    instance=new PeerCache(c,e);
  }
 else {
    if (instance.capacity != c || instance.expiryPeriod != e) {
      LOG.info("capacity and expiry periods already set to " + instance.capacity + " and "+ instance.expiryPeriod+ " respectively. Cannot set it to "+ c+ " and "+ e);
    }
  }
  return instance;
}
