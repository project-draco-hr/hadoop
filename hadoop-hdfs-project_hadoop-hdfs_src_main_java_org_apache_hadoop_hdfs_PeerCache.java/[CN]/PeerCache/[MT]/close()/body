{
  clear();
  if (daemon != null) {
    daemon.interrupt();
    try {
      daemon.join();
    }
 catch (    InterruptedException e) {
      throw new RuntimeException("failed to join thread");
    }
  }
  daemon=null;
}
