{
  Scan scan=new Scan(get);
  scan.setMaxVersions();
  RegionScanner scanner=null;
  try {
    scanner=new FlowScanner(region,scan.getBatch(),region.getScanner(scan));
    scanner.next(results);
    e.bypass();
  }
  finally {
    if (scanner != null) {
      scanner.close();
    }
  }
}
