{
  JournalRequestProto req=JournalRequestProto.newBuilder().setRegistration(PBHelper.convert(reg)).setFirstTxnId(firstTxnId).setNumTxns(numTxns).setRecords(PBHelper.getByteString(records)).build();
  try {
    rpcProxy.journal(NULL_CONTROLLER,req);
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
}
