{
  return isIn(datanodeSet,datanode.getPeerHostName(),datanode.getXferPort()) || isIn(datanodeSet,datanode.getIpAddr(),datanode.getXferPort()) || isIn(datanodeSet,datanode.getHostName(),datanode.getXferPort());
}
