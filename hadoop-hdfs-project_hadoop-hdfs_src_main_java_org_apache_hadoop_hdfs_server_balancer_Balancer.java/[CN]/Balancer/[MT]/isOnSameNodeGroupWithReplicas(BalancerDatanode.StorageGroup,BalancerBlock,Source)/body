{
  final DatanodeInfo targetDn=target.getDatanode();
  for (  BalancerDatanode.StorageGroup loc : block.locations) {
    if (loc != source && cluster.isOnSameNodeGroup(loc.getDatanode(),targetDn)) {
      return true;
    }
  }
  return false;
}
