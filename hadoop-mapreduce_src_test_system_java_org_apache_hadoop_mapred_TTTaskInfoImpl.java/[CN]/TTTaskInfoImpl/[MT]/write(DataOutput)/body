{
  out.writeBoolean(slotTaken);
  out.writeBoolean(wasKilled);
  conf.write(out);
  out.writeUTF(user);
  out.writeBoolean(isTaskCleanupTask);
  if (pid != null) {
    out.writeUTF(pid);
  }
 else {
    out.writeUTF("");
  }
  status.write(out);
}
