{
  final File f;
synchronized (this) {
    f=getFile(bpid,b.getBlockId());
  }
  if (f != null) {
    if (f.exists())     return f;
    datanode.checkDiskErrorAsync();
  }
  if (LOG.isDebugEnabled()) {
    LOG.debug("b=" + b + ", f="+ f);
  }
  return null;
}
