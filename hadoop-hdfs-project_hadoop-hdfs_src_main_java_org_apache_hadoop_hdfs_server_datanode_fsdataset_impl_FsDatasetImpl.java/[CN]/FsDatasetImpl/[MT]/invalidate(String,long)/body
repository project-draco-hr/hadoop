{
  if (datanode.getShortCircuitRegistry() != null) {
    datanode.getShortCircuitRegistry().processBlockInvalidation(new ExtendedBlockId(blockId,bpid));
    cacheManager.uncacheBlock(bpid,blockId);
  }
}
