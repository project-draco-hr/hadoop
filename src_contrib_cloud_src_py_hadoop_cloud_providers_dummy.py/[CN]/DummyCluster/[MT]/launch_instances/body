def launch_instances(self, role, number, image_id, size_id, instance_user_data, **kwargs):
    logger.info('launch_instances(%s, %s, %s, %s, %s, %s)', role, number, image_id, size_id, instance_user_data, str(kwargs))
    return [1]
