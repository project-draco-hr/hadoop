{
  DatanodeDescriptor dn=DFSTestUtil.getDatanodeDescriptor(ipAddr,DFSConfigKeys.DFS_DATANODE_DEFAULT_PORT,rackLocation);
  if (initializeStorage) {
    dn.updateStorage(new DatanodeStorage(DatanodeStorage.newStorageID()));
  }
  return dn;
}
