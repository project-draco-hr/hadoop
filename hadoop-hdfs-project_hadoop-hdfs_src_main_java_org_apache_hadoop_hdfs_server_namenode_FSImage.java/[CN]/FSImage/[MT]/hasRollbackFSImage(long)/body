{
  for (  StorageDirectory sd : storage.dirIterable(NameNodeDirType.IMAGE)) {
    final File rollbackImageFile=NNStorage.getStorageFile(sd,NameNodeFile.IMAGE_ROLLBACK,txid);
    if (rollbackImageFile.exists()) {
      return true;
    }
  }
  return false;
}
