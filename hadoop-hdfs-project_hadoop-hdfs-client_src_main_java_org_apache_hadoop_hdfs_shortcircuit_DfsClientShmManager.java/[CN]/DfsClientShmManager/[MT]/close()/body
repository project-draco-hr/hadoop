{
  lock.lock();
  try {
    if (closed)     return;
    closed=true;
  }
  finally {
    lock.unlock();
  }
  try {
    domainSocketWatcher.close();
  }
 catch (  Throwable e) {
    LOG.debug("Exception in closing " + domainSocketWatcher,e);
  }
}
