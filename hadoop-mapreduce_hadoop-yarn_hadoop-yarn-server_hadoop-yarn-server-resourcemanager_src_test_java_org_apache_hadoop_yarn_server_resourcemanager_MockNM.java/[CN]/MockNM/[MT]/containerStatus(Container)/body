{
  Map<ApplicationId,List<Container>> conts=new HashMap<ApplicationId,List<Container>>();
  conts.put(container.getId().getAppId(),Arrays.asList(new Container[]{container}));
  nodeHeartbeat(conts,true);
}
