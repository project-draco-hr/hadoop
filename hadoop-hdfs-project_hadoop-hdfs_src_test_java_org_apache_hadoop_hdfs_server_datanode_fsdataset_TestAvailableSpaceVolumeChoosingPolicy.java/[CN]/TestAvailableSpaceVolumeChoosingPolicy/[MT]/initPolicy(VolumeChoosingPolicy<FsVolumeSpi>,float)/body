{
  Configuration conf=new Configuration();
  conf.setLong(DFS_DATANODE_FSDATASET_VOLUME_CHOOSING_BALANCED_SPACE_THRESHOLD_KEY,1024 * 1024);
  conf.setFloat(DFS_DATANODE_FSDATASET_VOLUME_CHOOSING_BALANCED_SPACE_PREFERENCE_PERCENT_KEY,preferencePercent);
  ((Configurable)policy).setConf(conf);
}
