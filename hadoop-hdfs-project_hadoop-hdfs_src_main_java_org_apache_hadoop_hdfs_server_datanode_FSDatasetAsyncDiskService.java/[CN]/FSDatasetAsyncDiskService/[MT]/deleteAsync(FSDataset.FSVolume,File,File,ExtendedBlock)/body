{
  DataNode.LOG.info("Scheduling block " + block.getLocalBlock().toString() + " file "+ blockFile+ " for deletion");
  ReplicaFileDeleteTask deletionTask=new ReplicaFileDeleteTask(dataset,volume,blockFile,metaFile,block);
  execute(volume.getCurrentDir(),deletionTask);
}
