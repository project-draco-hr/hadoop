import sys
sampleText = 'Hello World!'
if (__name__ == '__main__'):
    args = sys.argv[1:]
    if (args[0] == '1'):
        sys.stdout.write(sampleText)
    elif (args[0] == '2'):
        sys.stderr.write(sampleText)
    pass
