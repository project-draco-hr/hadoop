{
  if (delay > 0) {
    nextBlockReportTime=monotonicNow() + ThreadLocalRandom.current().nextInt((int)(delay));
  }
 else {
    nextBlockReportTime=monotonicNow();
  }
  resetBlockReportTime=true;
  return nextBlockReportTime;
}
