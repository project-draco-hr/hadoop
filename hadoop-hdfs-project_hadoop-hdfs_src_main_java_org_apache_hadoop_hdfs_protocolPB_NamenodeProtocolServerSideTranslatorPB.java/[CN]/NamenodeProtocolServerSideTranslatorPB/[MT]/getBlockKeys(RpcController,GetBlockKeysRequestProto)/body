{
  ExportedBlockKeys keys;
  try {
    keys=impl.getBlockKeys();
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
  return GetBlockKeysResponseProto.newBuilder().setKeys(PBHelper.convert(keys)).build();
}
