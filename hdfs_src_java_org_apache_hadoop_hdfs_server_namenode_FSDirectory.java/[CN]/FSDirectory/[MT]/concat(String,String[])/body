{
  writeLock();
  try {
    waitForReady();
    long timestamp=now();
    unprotectedConcat(target,srcs,timestamp);
    fsImage.getEditLog().logConcat(target,srcs,timestamp);
  }
  finally {
    writeUnlock();
  }
}
