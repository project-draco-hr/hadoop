{
  super();
  try {
    Configuration conf=new Configuration();
    reinitialize(conf,new ContainerTokenSecretManager(conf),rmContext);
  }
 catch (  IOException ie) {
    LOG.info("add application failed with ",ie);
    assert(false);
  }
}
