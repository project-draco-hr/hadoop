{
  if (closed) {
    return;
  }
  dfsClient.checkOpen();
  if (blockReader != null) {
    blockReader.close(peerCache,fileInputStreamCache);
    blockReader=null;
  }
  super.close();
  fileInputStreamCache.close();
  closed=true;
}
