{
  PreemptionContainerProtoOrBuilder p=viaProto ? proto : builder;
  if (id != null) {
    return id;
  }
  if (!p.hasId()) {
    return null;
  }
  id=convertFromProtoFormat(p.getId());
  return id;
}
