{
  for (  Entry<Long,RollingWindowManager> entry : rollingWindowManagers.entrySet()) {
    if (!smallestOnly || smallestPeriod == entry.getKey()) {
      getMetrics(currTime,collector,entry.getKey(),entry.getValue(),all);
    }
  }
}
