{
  if (!isFinished()) {
    return 0;
  }
  long finishTime=0;
  for (  TaskAttempt at : attempts.values()) {
    if (finishTime < at.getFinishTime()) {
      finishTime=at.getFinishTime();
    }
  }
  return finishTime;
}
