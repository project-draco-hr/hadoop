def _parseMap(str):
    'Parses key value pairs'
    dict = {}
    for row in csv.reader([str], escapechar='\\', quoting=csv.QUOTE_NONE, doublequote=False):
        for f in row:
            [k, v] = f.split('=', 1)
            dict[k] = v
    return dict
