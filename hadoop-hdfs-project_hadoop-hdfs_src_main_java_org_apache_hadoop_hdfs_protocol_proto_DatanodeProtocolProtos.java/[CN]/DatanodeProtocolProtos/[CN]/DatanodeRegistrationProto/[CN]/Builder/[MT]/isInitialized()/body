{
  if (!hasDatanodeID()) {
    return false;
  }
  if (!hasStorateInfo()) {
    return false;
  }
  if (!hasKeys()) {
    return false;
  }
  if (!getDatanodeID().isInitialized()) {
    return false;
  }
  if (!getStorateInfo().isInitialized()) {
    return false;
  }
  if (!getKeys().isInitialized()) {
    return false;
  }
  return true;
}
