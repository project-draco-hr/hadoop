{
  checkNotNull(suite);
  checkNotNull(key);
  checkNotNull(iv);
  checkArgument(key.length == suite.getNumberBlockBits() / 8,"Unexpected key length");
  checkArgument(iv.length == suite.getNumberBlockBits() / 8,"Unexpected IV length");
  this.cipherSuite=suite;
  this.key=key;
  this.iv=iv;
}
