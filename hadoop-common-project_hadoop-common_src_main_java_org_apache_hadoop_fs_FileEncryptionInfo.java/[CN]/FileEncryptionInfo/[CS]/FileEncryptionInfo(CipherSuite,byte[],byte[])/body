{
  checkNotNull(suite);
  checkNotNull(key);
  checkNotNull(iv);
  checkArgument(key.length == suite.getAlgorithmBlockSize(),"Unexpected key length");
  checkArgument(iv.length == suite.getAlgorithmBlockSize(),"Unexpected IV length");
  this.cipherSuite=suite;
  this.key=key;
  this.iv=iv;
}
