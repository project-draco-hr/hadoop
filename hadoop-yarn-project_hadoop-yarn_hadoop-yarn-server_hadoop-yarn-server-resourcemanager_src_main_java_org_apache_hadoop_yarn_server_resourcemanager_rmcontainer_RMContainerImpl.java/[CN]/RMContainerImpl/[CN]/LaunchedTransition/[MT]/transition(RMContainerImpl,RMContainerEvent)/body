{
  container.logURL=WebAppUtils.getLogUrl(container.container.getNodeHttpAddress(),container.getAllocatedNode().toString(),container.containerId,container.user);
  container.containerAllocationExpirer.unregister(container.getContainerId());
}
