{
  Preconditions.checkState(lastSnapshotId == Snapshot.INVALID_ID || this.lastSnapshotId <= lastSnapshotId);
  final INode referred=this.getReferredINode().asReference().getReferredINode();
  return referred.computeQuotaUsage(counts,false,this.lastSnapshotId);
}
