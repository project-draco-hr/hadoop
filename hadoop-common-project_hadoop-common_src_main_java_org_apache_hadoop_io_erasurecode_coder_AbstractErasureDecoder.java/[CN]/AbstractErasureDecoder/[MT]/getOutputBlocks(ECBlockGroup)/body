{
  ECBlock[] outputBlocks=new ECBlock[getNumErasedBlocks(blockGroup.getDataBlocks())];
  int idx=0;
  for (int i=0; i < getNumDataUnits(); i++) {
    if (blockGroup.getDataBlocks()[i].isErased()) {
      outputBlocks[idx++]=blockGroup.getDataBlocks()[i];
    }
  }
  return outputBlocks;
}
