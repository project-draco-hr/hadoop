{
  ECBlock[] inputBlocks=new ECBlock[getNumParityUnits() + getNumDataUnits()];
  System.arraycopy(blockGroup.getParityBlocks(),0,inputBlocks,0,getNumParityUnits());
  System.arraycopy(blockGroup.getDataBlocks(),0,inputBlocks,getNumParityUnits(),getNumDataUnits());
  return inputBlocks;
}
