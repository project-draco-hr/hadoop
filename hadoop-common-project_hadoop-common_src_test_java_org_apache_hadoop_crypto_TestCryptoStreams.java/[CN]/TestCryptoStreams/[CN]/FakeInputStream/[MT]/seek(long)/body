{
  if (pos > length) {
    throw new IOException("Cannot seek after EOF.");
  }
  if (pos < 0) {
    throw new IOException("Cannot seek to negative offset.");
  }
  checkStream();
  this.pos=(int)pos;
}
