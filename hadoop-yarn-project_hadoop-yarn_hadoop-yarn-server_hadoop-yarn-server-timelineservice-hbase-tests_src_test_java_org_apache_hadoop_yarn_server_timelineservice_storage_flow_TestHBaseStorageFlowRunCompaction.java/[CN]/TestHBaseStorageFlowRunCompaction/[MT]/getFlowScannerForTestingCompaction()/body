{
  CompactionRequest request=new CompactionRequest();
  request.setIsMajor(true,true);
  FlowScanner fs=new FlowScanner(null,-1,null,(request.isMajor() == true ? FlowScannerOperation.MAJOR_COMPACTION : FlowScannerOperation.MINOR_COMPACTION));
  assertNotNull(fs);
  return fs;
}
