{
  if (!hasSrc()) {
    return false;
  }
  if (!hasClientName()) {
    return false;
  }
  if (!hasPrevious()) {
    return false;
  }
  if (!getPrevious().isInitialized()) {
    return false;
  }
  for (int i=0; i < getExcludeNodesCount(); i++) {
    if (!getExcludeNodes(i).isInitialized()) {
      return false;
    }
  }
  return true;
}
