{
  makeAvailable();
  if (available == 0) {
    return -1;
  }
  int ret=Math.min(len,available);
  for (int i=0; i < ret; ++i) {
    b[off + i]=xor[readPos + i];
  }
  readPos+=ret;
  available-=ret;
  return ret;
}
