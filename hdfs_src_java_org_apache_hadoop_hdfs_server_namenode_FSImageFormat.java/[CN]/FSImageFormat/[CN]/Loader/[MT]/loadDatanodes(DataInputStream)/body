{
  int imgVersion=getLayoutVersion();
  if (imgVersion > -3)   return;
  if (imgVersion <= -12) {
    return;
  }
  int size=in.readInt();
  for (int i=0; i < size; i++) {
    FSImageSerialization.DatanodeImage.skipOne(in);
  }
}
