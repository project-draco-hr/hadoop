{
  BlockInfo info=null;
  if (BlockIdManager.isStripedBlockID(block.getBlockId())) {
    info=blocksMap.getStoredBlock(new Block(BlockIdManager.convertToGroupID(block.getBlockId())));
  }
  if (info == null) {
    info=blocksMap.getStoredBlock(block);
  }
  return info;
}
