{
  JobConf jobConf=conf instanceof JobConf ? (JobConf)conf : new JobConf(conf);
  return jobConf.getMemoryRequired(taskType == TaskType.MAP ? org.apache.hadoop.mapreduce.TaskType.MAP : org.apache.hadoop.mapreduce.TaskType.REDUCE);
}
