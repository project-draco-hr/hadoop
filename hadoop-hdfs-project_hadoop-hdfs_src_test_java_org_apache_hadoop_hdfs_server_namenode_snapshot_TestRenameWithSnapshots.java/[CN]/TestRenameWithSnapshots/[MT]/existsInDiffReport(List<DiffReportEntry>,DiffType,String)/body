{
  for (  DiffReportEntry entry : entries) {
    if ((entry.getType() == type) && ((new String(entry.getRelativePath())).compareTo(relativePath) == 0)) {
      return true;
    }
  }
  return false;
}
