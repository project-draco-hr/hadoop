{
  String appIdString=appId.toString();
  AppLevelTimelineAggregator aggregator=new AppLevelTimelineAggregator(appIdString);
  return (aggregatorCollection.putIfAbsent(appIdString,aggregator) == aggregator);
}
