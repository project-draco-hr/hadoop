{
  assertTrue(doAnEdit());
  invalidateEditsDirAtIndex(0,true);
  assertExitInvocations(0);
  assertTrue(doAnEdit());
  assertExitInvocations(0);
  assertFalse(cluster.getNameNode().isInSafeMode());
}
