{
  logCliHelper.dumpAContainersLogsForALogType(request.getAppId().toString(),request.getContainerId().toString(),request.getNodeId(),request.getAppOwner(),logFiles != null ? Arrays.asList(logFiles) : null,localDir);
}
