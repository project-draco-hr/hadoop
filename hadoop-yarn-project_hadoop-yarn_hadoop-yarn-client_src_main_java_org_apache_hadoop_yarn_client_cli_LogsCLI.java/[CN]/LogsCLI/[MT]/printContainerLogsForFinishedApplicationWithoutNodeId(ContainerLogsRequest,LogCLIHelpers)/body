{
  ContainerLogsRequest newOptions=getMatchedLogOptions(request,logCliHelper);
  if (newOptions == null) {
    return -1;
  }
  return logCliHelper.dumpAContainersLogsForALogTypeWithoutNodeId(newOptions);
}
