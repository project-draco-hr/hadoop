{
  if (!request.isAppFinished()) {
    System.err.println("The -list_nodes command can be only used with " + "finished applications");
    return -1;
  }
 else {
    logCliHelper.printNodesList(request.getAppId(),request.getAppOwner(),System.out,System.err);
    return 0;
  }
}
