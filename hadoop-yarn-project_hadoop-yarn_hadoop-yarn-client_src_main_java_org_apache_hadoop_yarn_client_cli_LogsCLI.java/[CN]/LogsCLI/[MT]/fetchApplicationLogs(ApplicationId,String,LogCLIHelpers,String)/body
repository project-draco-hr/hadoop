{
  int resultCode=logCliHelper.dumpAllContainersLogs(appId,appOwner,localDir);
  if (resultCode == -1) {
    System.err.println("Can not find the logs for the application: " + appId + " with the appOwner: "+ appOwner);
  }
  return resultCode;
}
