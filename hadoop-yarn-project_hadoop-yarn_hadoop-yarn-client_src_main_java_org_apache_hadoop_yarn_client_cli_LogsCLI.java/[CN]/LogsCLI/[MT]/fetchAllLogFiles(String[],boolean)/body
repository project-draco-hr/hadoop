{
  if (logFiles == null || logFiles.length == 0) {
    return true;
  }
  List<String> logs=Arrays.asList(logFiles);
  if (logs.contains("ALL") || logs.contains("*") || (logs.contains(".*") && useRegex)) {
    return true;
  }
  return false;
}
