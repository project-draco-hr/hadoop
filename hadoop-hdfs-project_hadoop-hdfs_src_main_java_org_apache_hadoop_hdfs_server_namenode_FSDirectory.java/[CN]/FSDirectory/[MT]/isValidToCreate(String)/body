{
  String srcs=normalizePath(src);
  readLock();
  try {
    if (srcs.startsWith("/") && !srcs.endsWith("/") && rootDir.getMutableNode(srcs,false) == null) {
      return true;
    }
 else {
      return false;
    }
  }
  finally {
    readUnlock();
  }
}
