{
  writeLock();
  try {
    replaceINodeUnsynced(path,oldnode,newnode);
    for (    INode i : newnode.getChildrenList(null)) {
      i.parent=newnode;
    }
  }
  finally {
    writeUnlock();
  }
}
