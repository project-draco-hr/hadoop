{
  Preconditions.checkState(!ready,"FSDirectory already loaded");
  writeLock();
  try {
    setReady(true);
    this.nameCache.initialized();
    cond.signalAll();
  }
  finally {
    writeUnlock();
  }
}
