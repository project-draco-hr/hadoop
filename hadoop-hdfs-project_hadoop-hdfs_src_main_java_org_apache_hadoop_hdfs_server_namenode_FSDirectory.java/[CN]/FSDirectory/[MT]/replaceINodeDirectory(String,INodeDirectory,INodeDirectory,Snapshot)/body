{
  writeLock();
  try {
    replaceINodeUnsynced(path,oldnode,newnode,latestSnapshot);
    for (    INode i : newnode.getChildrenList(null)) {
      i.parent=newnode;
    }
  }
  finally {
    writeUnlock();
  }
}
