{
  if (getFSNamesystem().isImageLoaded() && !inode.isInLatestSnapshot(iip.getLatestSnapshotId())) {
    Quota.Counts counts=inode.computeQuotaUsage();
    unprotectedUpdateCount(iip,iip.length() - 1,-counts.get(Quota.NAMESPACE),-counts.get(Quota.DISKSPACE));
  }
}
