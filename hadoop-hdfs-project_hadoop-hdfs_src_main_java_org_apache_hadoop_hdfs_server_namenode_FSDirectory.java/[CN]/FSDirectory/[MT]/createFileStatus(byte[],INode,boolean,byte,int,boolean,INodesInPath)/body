{
  if (needLocation) {
    return createLocatedFileStatus(path,node,storagePolicy,snapshot,isRawPath,iip);
  }
 else {
    return createFileStatus(path,node,storagePolicy,snapshot,isRawPath,iip);
  }
}
