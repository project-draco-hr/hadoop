{
  final int nComponents=(pathComponents == null) ? 0 : pathComponents.length;
  if (nComponents <= 2) {
    return src;
  }
  if (!Arrays.equals(DOT_RESERVED,pathComponents[1])) {
    return src;
  }
  if (Arrays.equals(DOT_INODES,pathComponents[2])) {
    if (nComponents > 3) {
      return resolveDotInodesPath(src,pathComponents,fsd);
    }
 else {
      return src;
    }
  }
 else   if (Arrays.equals(RAW,pathComponents[2])) {
    if (nComponents == 3) {
      return Path.SEPARATOR;
    }
 else {
      if (nComponents == 4 && Arrays.equals(DOT_RESERVED,pathComponents[3])) {
        return src;
      }
 else {
        return constructRemainingPath("",pathComponents,3);
      }
    }
  }
 else {
    return src;
  }
}
