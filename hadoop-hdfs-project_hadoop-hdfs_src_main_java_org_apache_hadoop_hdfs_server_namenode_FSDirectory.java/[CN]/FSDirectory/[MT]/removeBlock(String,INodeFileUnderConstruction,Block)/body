{
  waitForReady();
  writeLock();
  try {
    unprotectedRemoveBlock(path,fileNode,block);
    fsImage.getEditLog().logOpenFile(path,fileNode);
  }
  finally {
    writeUnlock();
  }
  return true;
}
