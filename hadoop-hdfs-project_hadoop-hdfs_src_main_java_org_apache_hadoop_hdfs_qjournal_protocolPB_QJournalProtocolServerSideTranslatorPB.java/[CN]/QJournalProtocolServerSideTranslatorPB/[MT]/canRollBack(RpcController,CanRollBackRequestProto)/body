{
  try {
    Boolean result=impl.canRollBack(convert(request.getJid()),PBHelper.convert(request.getStorage()),PBHelper.convert(request.getPrevStorage()),request.getTargetLayoutVersion());
    return CanRollBackResponseProto.newBuilder().setCanRollBack(result).build();
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
}
