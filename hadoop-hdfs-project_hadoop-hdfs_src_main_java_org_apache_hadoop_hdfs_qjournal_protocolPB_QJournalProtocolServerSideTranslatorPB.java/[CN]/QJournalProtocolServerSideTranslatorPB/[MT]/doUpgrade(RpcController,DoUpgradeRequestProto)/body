{
  try {
    impl.doUpgrade(convert(request.getJid()),PBHelper.convert(request.getSInfo()));
    return DoUpgradeResponseProto.getDefaultInstance();
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
}
