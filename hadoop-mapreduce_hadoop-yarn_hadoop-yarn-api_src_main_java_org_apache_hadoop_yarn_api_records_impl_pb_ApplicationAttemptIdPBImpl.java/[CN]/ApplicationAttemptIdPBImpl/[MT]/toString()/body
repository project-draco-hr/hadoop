{
  String id=(this.getApplicationId() != null) ? this.getApplicationId().getClusterTimestamp() + "_" + idFormat.format(this.getApplicationId().getId()) : "none";
  return "appattempt_" + id + "_"+ counterFormat.format(getAttemptId());
}
