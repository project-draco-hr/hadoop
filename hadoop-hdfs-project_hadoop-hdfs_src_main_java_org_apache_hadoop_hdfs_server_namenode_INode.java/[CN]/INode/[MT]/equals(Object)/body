{
  if (this == that) {
    return true;
  }
  if (that == null || !(that instanceof INode)) {
    return false;
  }
  return Arrays.equals(this.getLocalNameBytes(),((INode)that).getLocalNameBytes());
}
