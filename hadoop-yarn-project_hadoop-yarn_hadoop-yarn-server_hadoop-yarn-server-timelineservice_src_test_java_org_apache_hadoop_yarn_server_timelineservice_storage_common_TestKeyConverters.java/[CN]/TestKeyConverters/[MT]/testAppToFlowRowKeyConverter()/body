{
  byte[] byteRowKey=AppToFlowRowKeyConverter.getInstance().encode(new AppToFlowRowKey(CLUSTER,APPLICATION_ID));
  AppToFlowRowKey rowKey=AppToFlowRowKeyConverter.getInstance().decode(byteRowKey);
  assertEquals(CLUSTER,rowKey.getClusterId());
  assertEquals(APPLICATION_ID,rowKey.getAppId());
}
