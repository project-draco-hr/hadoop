{
  if (shouldSlow) {
    while (fastPingCounter < 2) {
      try {
        wait();
      }
 catch (      InterruptedException ignored) {
      }
    }
    fastPingCounter-=2;
  }
 else {
    fastPingCounter++;
    notify();
  }
}
