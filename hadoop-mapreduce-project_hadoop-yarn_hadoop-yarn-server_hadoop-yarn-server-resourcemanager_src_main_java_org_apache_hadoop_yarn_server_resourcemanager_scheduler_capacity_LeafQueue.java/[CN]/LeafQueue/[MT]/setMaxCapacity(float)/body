{
  CSQueueUtils.checkMaxCapacity(getQueueName(),capacity,maximumCapacity);
  this.maximumCapacity=maximumCapacity;
  this.absoluteMaxCapacity=(Math.round(maximumCapacity * 100) == CapacitySchedulerConfiguration.UNDEFINED) ? Float.MAX_VALUE : (parent.getAbsoluteCapacity() * maximumCapacity);
}
