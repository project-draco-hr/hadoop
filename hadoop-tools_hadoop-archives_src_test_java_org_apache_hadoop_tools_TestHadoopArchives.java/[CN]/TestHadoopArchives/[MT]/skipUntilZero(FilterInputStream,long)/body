{
  long skipped=0;
  long remainsToSkip=toSkip;
  long s;
  while (skipped < toSkip) {
    s=fis.skip(remainsToSkip);
    if (s == 0) {
      return skipped;
    }
    skipped+=s;
    remainsToSkip-=s;
  }
  return skipped;
}
