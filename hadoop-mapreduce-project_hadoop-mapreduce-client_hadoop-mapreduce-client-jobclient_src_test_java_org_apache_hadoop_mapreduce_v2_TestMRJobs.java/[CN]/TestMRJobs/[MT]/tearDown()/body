{
  if (mrCluster != null) {
    mrCluster.stop();
    mrCluster=null;
  }
  if (dfsCluster != null) {
    dfsCluster.shutdown();
    dfsCluster=null;
  }
  if (localFs.exists(TEST_RESOURCES_DIR)) {
    localFs.delete(TEST_RESOURCES_DIR,true);
  }
}
