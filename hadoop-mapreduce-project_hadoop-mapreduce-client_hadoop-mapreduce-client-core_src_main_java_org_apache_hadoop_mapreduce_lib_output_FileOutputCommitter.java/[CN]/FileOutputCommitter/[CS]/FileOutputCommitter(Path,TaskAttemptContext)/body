{
  if (outputPath != null) {
    this.outputPath=outputPath;
    outputFileSystem=outputPath.getFileSystem(context.getConfiguration());
    workPath=new Path(outputPath,getTaskAttemptBaseDirName(context)).makeQualified(outputFileSystem);
  }
}
