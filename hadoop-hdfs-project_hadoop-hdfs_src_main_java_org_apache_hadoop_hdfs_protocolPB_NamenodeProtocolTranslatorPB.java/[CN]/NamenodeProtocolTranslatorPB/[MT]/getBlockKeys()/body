{
  try {
    GetBlockKeysResponseProto rsp=rpcProxy.getBlockKeys(NULL_CONTROLLER,GET_BLOCKKEYS);
    return rsp.hasKeys() ? PBHelper.convert(rsp.getKeys()) : null;
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
}
