{
  File file=getDumpTreeFile(testDir,fileSuffix);
  PrintWriter out=new PrintWriter(new FileWriter(file,false),true);
  fsn.getFSDirectory().getINode(rootDir.toString()).dumpTreeRecursively(out,new StringBuilder(),null);
  out.close();
  return file;
}
