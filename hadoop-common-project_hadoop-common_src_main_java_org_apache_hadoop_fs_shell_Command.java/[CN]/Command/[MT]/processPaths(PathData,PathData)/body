{
  for (  PathData item : items) {
    try {
      processPath(item);
      if (recursive && item.stat.isDirectory()) {
        recursePath(item);
      }
      postProcessPath(item);
    }
 catch (    IOException e) {
      displayError(e);
    }
  }
}
