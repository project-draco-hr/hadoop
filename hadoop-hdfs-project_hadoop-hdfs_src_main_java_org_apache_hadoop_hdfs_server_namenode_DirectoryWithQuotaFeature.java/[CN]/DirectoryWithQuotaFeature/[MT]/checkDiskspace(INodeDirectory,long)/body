{
  if (-1 != quota.getDiskSpace() && usage.getDiskSpace() != computed) {
    NameNode.LOG.error("BUG: Inconsistent diskspace for directory " + dir.getFullPathName() + ". Cached = "+ usage.getDiskSpace()+ " != Computed = "+ computed);
  }
}
