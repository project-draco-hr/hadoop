{
  parent=null;
  if (blocks != null && info != null) {
    for (    BlockInfo blk : blocks) {
      info.addDeleteBlock(blk);
      blk.setBlockCollection(null);
    }
  }
  setBlocks(null);
  return 1;
}
