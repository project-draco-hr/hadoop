{
  if (blks == null) {
    return;
  }
  for (int i=0; i < blks.length; i++) {
    Preconditions.checkState(blks[i].isComplete(),"Failed to finalize" + " %s %s since blocks[%s] is non-complete, where blocks=%s.",getClass().getSimpleName(),this,i,Arrays.asList(blks));
  }
}
