{
  if (LOG.isDebugEnabled()) {
    LOG.debug("SUCCESS - TIMELINE V2 PROTOTYPE");
    LOG.debug("postEntities(entities=" + entities + ", callerUgi="+ callerUgi+ ")");
  }
  return writer.write(entities);
}
