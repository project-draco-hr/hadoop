{
  try (RandomAccessFile file=new RandomAccessFile(to,"rws");FileOutputStream out=new FileOutputStream(file.getFD())){
    file.seek(0);
    props.store(out,null);
    file.setLength(out.getChannel().position());
  }
 }
