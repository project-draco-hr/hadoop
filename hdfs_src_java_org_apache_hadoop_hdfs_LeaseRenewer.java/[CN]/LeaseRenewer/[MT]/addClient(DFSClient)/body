{
  for (  DFSClient c : dfsclients) {
    if (c == dfsc) {
      return;
    }
  }
  dfsclients.add(dfsc);
  if (dfsc.getHdfsTimeout() > 0) {
    final long half=dfsc.getHdfsTimeout() / 2;
    if (half < renewal) {
      this.renewal=half;
    }
  }
}
