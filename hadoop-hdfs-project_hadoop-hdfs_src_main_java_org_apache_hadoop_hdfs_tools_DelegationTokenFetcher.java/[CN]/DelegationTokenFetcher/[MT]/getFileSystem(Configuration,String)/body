{
  if (url == null) {
    return FileSystem.get(conf);
  }
  URI fsUri=URI.create(url.replaceFirst("^http://",WebHdfsFileSystem.SCHEME + "://").replaceFirst("^https://",SWebHdfsFileSystem.SCHEME + "://"));
  return FileSystem.get(fsUri,conf);
}
