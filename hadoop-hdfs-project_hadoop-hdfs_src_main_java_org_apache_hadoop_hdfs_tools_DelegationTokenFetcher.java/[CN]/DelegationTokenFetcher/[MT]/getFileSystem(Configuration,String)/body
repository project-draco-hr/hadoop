{
  if (url == null) {
    return FileSystem.get(conf);
  }
  URI fsUri=URI.create(url.replaceFirst("^http://",WebHdfsConstants.WEBHDFS_SCHEME + "://").replaceFirst("^https://",WebHdfsConstants.SWEBHDFS_SCHEME + "://"));
  return FileSystem.get(fsUri,conf);
}
