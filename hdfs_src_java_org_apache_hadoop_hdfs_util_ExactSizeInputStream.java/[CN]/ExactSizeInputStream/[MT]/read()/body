{
  if (remaining <= 0) {
    return -1;
  }
  final int result=super.read();
  if (result >= 0) {
    --remaining;
  }
 else   if (remaining > 0) {
    throw new EOFException("Premature EOF. Expected " + remaining + "more bytes");
  }
  return result;
}
