{
  Configuration conf=getConfig();
  final YarnRPC rpc=YarnRPC.create(conf);
  return (CollectorNodemanagerProtocol)rpc.getProxy(CollectorNodemanagerProtocol.class,nmCollectorServiceAddress,conf);
}
