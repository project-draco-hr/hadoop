{
  if (collectors != null && collectors.size() > 1) {
    for (    TimelineCollector c : collectors.values()) {
      c.serviceStop();
    }
  }
  if (writer != null) {
    writer.close();
  }
  super.serviceStop();
}
