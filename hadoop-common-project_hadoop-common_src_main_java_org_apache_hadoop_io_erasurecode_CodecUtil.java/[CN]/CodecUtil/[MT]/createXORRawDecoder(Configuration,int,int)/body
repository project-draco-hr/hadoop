{
  Preconditions.checkNotNull(conf);
  RawErasureCoder rawCoder=createRawCoder(conf,getFactNameFromCodec(conf,ErasureCodeConstants.XOR_CODEC_NAME),false,numDataUnits,numParityUnits);
  return (RawErasureDecoder)rawCoder;
}
