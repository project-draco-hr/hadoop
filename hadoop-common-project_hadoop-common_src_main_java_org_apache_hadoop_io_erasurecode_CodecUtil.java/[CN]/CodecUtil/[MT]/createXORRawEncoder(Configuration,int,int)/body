{
  Preconditions.checkNotNull(conf);
  RawErasureCoder rawCoder=createRawCoder(conf,getFactNameFromCodec(conf,ErasureCodeConstants.XOR_CODEC_NAME),true,numDataUnits,numParityUnits);
  return (RawErasureEncoder)rawCoder;
}
