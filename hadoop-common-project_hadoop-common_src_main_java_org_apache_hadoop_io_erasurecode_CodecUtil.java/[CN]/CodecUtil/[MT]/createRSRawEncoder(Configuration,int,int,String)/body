{
  Preconditions.checkNotNull(conf);
  if (codec == null) {
    codec=ErasureCodeConstants.RS_DEFAULT_CODEC_NAME;
  }
  RawErasureCoder rawCoder=createRawCoder(conf,getFactNameFromCodec(conf,codec),true,numDataUnits,numParityUnits);
  return (RawErasureEncoder)rawCoder;
}
