{
  TimelineEntityReader reader=TimelineEntityReaderFactory.createSingleEntityReader(userId,clusterId,flowId,flowRunId,appId,entityType,entityId,confsToRetrieve,metricsToRetrieve,fieldsToRetrieve);
  return reader.readEntity(hbaseConf,conn);
}
