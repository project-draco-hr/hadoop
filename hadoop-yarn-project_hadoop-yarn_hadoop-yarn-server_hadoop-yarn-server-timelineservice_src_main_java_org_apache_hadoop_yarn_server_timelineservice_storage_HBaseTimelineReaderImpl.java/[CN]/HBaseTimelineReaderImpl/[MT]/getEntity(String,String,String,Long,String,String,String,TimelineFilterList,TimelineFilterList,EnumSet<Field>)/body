{
  TimelineEntityReader reader=TimelineEntityReaderFactory.createSingleEntityReader(userId,clusterId,flowName,flowRunId,appId,entityType,entityId,confsToRetrieve,metricsToRetrieve,fieldsToRetrieve);
  return reader.readEntity(hbaseConf,conn);
}
