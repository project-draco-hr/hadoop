def start(self):
    ' Start XML-RPC server and register service.'
    self.logs['main'].info(('Starting HOD service: %s ...' % self.name))
    if self._xrc:
        self._xrc.serve_forever()
    if (self._cfg.has_key('register') and self._cfg['register']):
        self._register_service()
