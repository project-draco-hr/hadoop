def stop(self):
    ' Stop XML-RPC server, unregister service and set stop flag. '
    self.logs['main'].info('Stopping service...')
    if self._xrc:
        self._xrc.stop()
    self.__stop = True
    return True
