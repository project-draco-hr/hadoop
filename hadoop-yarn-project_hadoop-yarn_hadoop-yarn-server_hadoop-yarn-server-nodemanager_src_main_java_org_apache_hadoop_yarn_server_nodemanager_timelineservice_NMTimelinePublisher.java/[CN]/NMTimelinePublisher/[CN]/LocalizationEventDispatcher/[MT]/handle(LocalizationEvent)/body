{
switch (event.getType()) {
case INIT_CONTAINER_RESOURCES:
case CONTAINER_RESOURCES_LOCALIZED:
    break;
default :
  LOG.error("Seems like event type is captured only in " + "publishLocalizationEvent method and not handled here");
break;
}
}
