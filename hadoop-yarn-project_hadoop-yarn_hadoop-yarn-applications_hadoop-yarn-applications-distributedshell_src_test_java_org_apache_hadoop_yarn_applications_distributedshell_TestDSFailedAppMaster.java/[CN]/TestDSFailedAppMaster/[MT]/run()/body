{
  boolean res=super.run();
  if (appAttemptID.getAttemptId() == 2) {
    if (numAllocatedContainers.get() != 1 || numRequestedContainers.get() != 0) {
      LOG.info("Application Master failed. exiting");
      System.exit(200);
    }
  }
  return res;
}
