{
  bootstrap=startHttpServer(httpPort,testToken,serviceUrl);
  assertTrue("testRenewTokenFromHttp error",Long.valueOf(EXP_DATE) == DelegationTokenFetcher.renewDelegationToken(serviceUrl,testToken));
  if (assertionError != null)   throw assertionError;
}
