{
  String appIdString=parseApplicationId(appIdToParse);
  final TimelineCollectorManager collectorManager=(TimelineCollectorManager)context.getAttribute(TimelineCollectorManager.COLLECTOR_MANAGER_ATTR_KEY);
  return collectorManager.get(appIdString);
}
