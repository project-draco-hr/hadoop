{
  return new TaskCleaner(){
    @Override public void handle(    TaskCleanupEvent event){
      getContext().getEventHandler().handle(new TaskAttemptEvent(event.getAttemptID(),TaskAttemptEventType.TA_CLEANUP_DONE));
    }
  }
;
}
