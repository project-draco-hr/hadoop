{
  final long counter=streamOffset / codec.getCipherSuite().getAlgorithmBlockSize();
  padding=(byte)(streamOffset % codec.getCipherSuite().getAlgorithmBlockSize());
  inBuffer.position(padding);
  codec.calculateIV(initIV,counter,iv);
  encryptor.init(key,iv);
}
