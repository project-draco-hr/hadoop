{
  final String providerUriStr=conf.getTrimmed(HdfsClientConfigKeys.DFS_ENCRYPTION_KEY_PROVIDER_URI,"");
  if (providerUriStr.isEmpty()) {
    LOG.error("Could not find uri with key [" + HdfsClientConfigKeys.DFS_ENCRYPTION_KEY_PROVIDER_URI + "] to create a keyProvider !!");
    return null;
  }
  final URI providerUri;
  try {
    providerUri=new URI(providerUriStr);
  }
 catch (  URISyntaxException e) {
    LOG.error("KeyProvider URI string is invalid [" + providerUriStr + "]!!",e.getCause());
    return null;
  }
  return providerUri;
}
