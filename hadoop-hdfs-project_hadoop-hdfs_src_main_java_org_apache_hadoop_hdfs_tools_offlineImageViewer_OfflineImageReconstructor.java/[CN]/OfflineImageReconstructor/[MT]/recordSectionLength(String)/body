{
  long curSectionStartOffset=sectionStartOffset;
  long curPos=out.getCount();
  fileSummaryBld.addSections(FileSummary.Section.newBuilder().setName(sectionNamePb).setLength(curPos - curSectionStartOffset).setOffset(curSectionStartOffset));
  sectionStartOffset=curPos;
}
