{
  String info="<price>" + Float.toString(totalSpending) + "</price>\n";
  for (  BudgetQueue queue : store.getQueues()) {
    info+="<queue name=\"" + queue.name + "\">"+ queueInfo.get(queue.name)+ "</queue>\n";
  }
  return info;
}
