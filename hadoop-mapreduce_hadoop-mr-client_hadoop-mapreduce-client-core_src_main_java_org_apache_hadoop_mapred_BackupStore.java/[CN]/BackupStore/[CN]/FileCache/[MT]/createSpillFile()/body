{
  Path tmp=new Path(Constants.OUTPUT + "/backup_" + tid.getId()+ "_"+ (spillNumber++)+ ".out");
  LOG.info("Created file: " + tmp);
  file=lDirAlloc.getLocalPathForWrite(tmp.toUri().getPath(),-1,conf);
  return new Writer<K,V>(conf,fs,file);
}
