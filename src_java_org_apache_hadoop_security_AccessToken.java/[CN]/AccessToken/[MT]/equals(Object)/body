{
  if (obj == this) {
    return true;
  }
  if (obj instanceof AccessToken) {
    AccessToken that=(AccessToken)obj;
    return isEqual(this.tokenID,that.tokenID) && isEqual(this.tokenAuthenticator,that.tokenAuthenticator);
  }
  return false;
}
