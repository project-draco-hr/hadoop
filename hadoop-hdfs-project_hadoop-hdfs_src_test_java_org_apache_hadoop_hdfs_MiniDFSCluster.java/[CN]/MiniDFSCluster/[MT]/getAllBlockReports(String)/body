{
  int numDataNodes=dataNodes.size();
  final List<Map<String,BlockListAsLongs>> result=new ArrayList<Map<String,BlockListAsLongs>>(numDataNodes);
  for (int i=0; i < numDataNodes; ++i) {
    result.add(getBlockReport(bpid,i));
  }
  return result;
}
