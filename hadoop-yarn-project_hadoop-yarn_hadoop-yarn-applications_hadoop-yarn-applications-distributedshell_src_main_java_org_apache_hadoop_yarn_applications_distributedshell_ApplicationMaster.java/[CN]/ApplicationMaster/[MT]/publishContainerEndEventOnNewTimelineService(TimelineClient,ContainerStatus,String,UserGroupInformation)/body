{
  Runnable publishWrapper=new Runnable(){
    public void run(){
      publishContainerEndEventOnNewTimelineServiceBase(timelineClient,container,domainId,ugi);
    }
  }
;
  threadPool.execute(publishWrapper);
}
