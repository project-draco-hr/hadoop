def __tostring_keyval(self, value):
    string = '"'
    for key in value:
        val = re.sub('\\\\\\\\', '\\\\\\\\\\\\\\\\', value[key])
        string = ('%s%s=%s,' % (string, key, val))
    return (string[:(-1)] + '"')
