def __norm_range(self, value):
    range = reDash.split(value)
    if (len(range) > 1):
        start = int(range[0])
        end = int(range[1])
    else:
        start = int(range[0])
        end = None
    return (start, end)
