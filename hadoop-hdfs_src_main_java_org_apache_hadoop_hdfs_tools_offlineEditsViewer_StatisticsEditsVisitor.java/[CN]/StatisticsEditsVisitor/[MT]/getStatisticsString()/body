{
  StringBuffer sb=new StringBuffer();
  for (  FSEditLogOpCodes opCode : FSEditLogOpCodes.values()) {
    sb.append(String.format("    %-30.30s (%3d): %d%n",opCode,opCode.getOpCode(),opCodeCount.get(opCode)));
  }
  return sb.toString();
}
