{
  Client client=createClient();
  try {
    URI uri=URI.create("http://localhost:" + serverPort + "/ws/v2/"+ "timeline/app/cluster1/flow_name/1002345678919/"+ "application_1111111111_1378?userid=user1");
    verifyHttpResponse(client,uri,Status.NOT_FOUND);
  }
  finally {
    client.destroy();
  }
}
