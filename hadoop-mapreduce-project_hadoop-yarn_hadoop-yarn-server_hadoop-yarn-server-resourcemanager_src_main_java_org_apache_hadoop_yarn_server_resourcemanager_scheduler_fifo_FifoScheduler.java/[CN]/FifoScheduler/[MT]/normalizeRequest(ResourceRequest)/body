{
  int memory=ask.getCapability().getMemory();
  memory=MINIMUM_MEMORY * ((memory / MINIMUM_MEMORY) + (memory % MINIMUM_MEMORY > 0 ? 1 : 0));
  ask.setCapability(Resources.createResource(memory));
}
