{
  if (System.currentTimeMillis() - waitStartTime <= rmStartIntervalMS || rmNeverStart) {
    throw new YarnException("Faking RM start failure as start " + "delay timer has not expired.");
  }
 else {
    return resourceTracker;
  }
}
