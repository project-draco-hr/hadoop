def __attr__(self, attrname):
    'loggerNames  - list of defined logger names'
    if (attrname == 'loggerNames'):
        return self.__loggerNames.keys()
    else:
        raise AttributeError, attrname
