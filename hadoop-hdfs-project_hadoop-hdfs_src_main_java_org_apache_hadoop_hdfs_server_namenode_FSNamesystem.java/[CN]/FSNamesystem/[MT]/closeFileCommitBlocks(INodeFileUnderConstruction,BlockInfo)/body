{
  String src=leaseManager.findPath(pendingFile);
  commitOrCompleteLastBlock(pendingFile,storedBlock);
  finalizeINodeFileUnderConstruction(src,pendingFile,Snapshot.findLatestSnapshot(pendingFile,null));
  return src;
}
