{
  if (!haContext.getState().shouldPopulateReplQueues()) {
    return false;
  }
  SafeModeInfo safeMode=this.safeMode;
  if (safeMode == null)   return true;
  return safeMode.isPopulatingReplQueues();
}
