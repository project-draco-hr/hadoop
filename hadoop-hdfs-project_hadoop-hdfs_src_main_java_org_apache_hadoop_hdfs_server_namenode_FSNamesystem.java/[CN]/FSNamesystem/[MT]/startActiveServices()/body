{
  LOG.info("Starting services required for active state");
  writeLock();
  try {
    startSecrectManager();
    lmthread=new Daemon(leaseManager.new Monitor());
    lmthread.start();
  }
  finally {
    writeUnlock();
  }
}
