{
  String src=leaseManager.findPath(pendingFile);
  commitOrCompleteLastBlock(pendingFile,storedBlock);
  finalizeINodeFileUnderConstruction(src,pendingFile,Snapshot.findLatestSnapshot(pendingFile,Snapshot.CURRENT_STATE_ID));
  return src;
}
