{
  checkOperation(OperationCategory.WRITE);
  writeLock();
  try {
    checkOperation(OperationCategory.WRITE);
    checkNameNodeSafeMode("Cannot remove acl on " + src);
    dir.removeAcl(src);
  }
  finally {
    writeUnlock();
  }
}
