{
  if (!imageLoaded) {
    writeLock();
    try {
      while (!imageLoaded) {
        try {
          cond.await(5000,TimeUnit.MILLISECONDS);
        }
 catch (        InterruptedException ignored) {
        }
      }
    }
  finally {
      writeUnlock();
    }
  }
}
