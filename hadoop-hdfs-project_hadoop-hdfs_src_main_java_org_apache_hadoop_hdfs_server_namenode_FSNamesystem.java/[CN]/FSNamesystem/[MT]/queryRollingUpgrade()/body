{
  checkSuperuserPrivilege();
  checkOperation(OperationCategory.READ);
  readLock();
  try {
    return rollingUpgradeInfo != null ? rollingUpgradeInfo : RollingUpgradeInfo.EMPTY_INFO;
  }
  finally {
    readUnlock();
  }
}
