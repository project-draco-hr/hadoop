{
  return Resources.createResource(roundUp(Math.max(r.getMemory(),minimumResource.getMemory()),minimumResource.getMemory()),roundUp(Math.max(r.getVirtualCores(),minimumResource.getVirtualCores()),minimumResource.getVirtualCores()));
}
