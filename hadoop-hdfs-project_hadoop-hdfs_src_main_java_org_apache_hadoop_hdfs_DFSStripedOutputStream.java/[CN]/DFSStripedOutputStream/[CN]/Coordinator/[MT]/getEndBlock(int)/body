{
  try {
    return endBlocks.get(i).poll(conf.getStripedWriteMaxSecondsGetEndedBlock(),TimeUnit.SECONDS);
  }
 catch (  InterruptedException e) {
    throw DFSUtil.toInterruptedIOException("getEndBlock interrupted, i=" + i,e);
  }
}
