{
  try {
    thread.preventCheckpointsFor(PREVENT_AFTER_CANCEL_MS);
    namesystem.getFSImage().cancelSaveNamespace("About to exit standby state");
  }
 catch (  InterruptedException e) {
    throw new ServiceFailedException("Interrupted while trying to cancel checkpoint");
  }
}
