{
  ArrayList<List<String>> addressList=new ArrayList<List<String>>();
  if (address != null) {
    addressList.add(address);
  }
  return new Endpoint(api,AddressTypes.ADDRESS_HOSTNAME_AND_PORT,protobuf ? ProtocolTypes.PROTOCOL_HADOOP_IPC_PROTOBUF : ProtocolTypes.PROTOCOL_HADOOP_IPC,addressList);
}
