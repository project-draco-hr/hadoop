{
  protocol=Text.readString(in);
  if (protocol.isEmpty()) {
    protocol=null;
  }
  boolean ugiPresent=in.readBoolean();
  if (ugiPresent) {
    ugi.readFields(in);
  }
 else {
    ugi=null;
  }
}
