{
  super(ctx);
  this.app=app;
  set(APP_ID,app.context.getApplicationID().toString());
  set(RM_WEB,JOINER.join(WebAppUtil.getSchemePrefix(),WebAppUtils.getResolvedRMWebAppURLWithoutScheme(conf)));
}
