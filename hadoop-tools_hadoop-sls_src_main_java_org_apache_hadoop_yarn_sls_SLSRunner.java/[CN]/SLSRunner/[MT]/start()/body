{
  startRM();
  startNM();
  startAM();
  ((SchedulerWrapper)rm.getResourceScheduler()).setQueueSet(this.queueAppNumMap.keySet());
  ((SchedulerWrapper)rm.getResourceScheduler()).setTrackedAppSet(this.trackedApps);
  printSimulationInfo();
  waitForNodesRunning();
  runner.start();
}
