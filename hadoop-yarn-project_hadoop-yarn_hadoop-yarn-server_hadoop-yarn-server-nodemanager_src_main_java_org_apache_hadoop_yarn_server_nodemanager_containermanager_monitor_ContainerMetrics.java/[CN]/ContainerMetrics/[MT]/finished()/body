{
  if (!finished) {
    this.finished=true;
    if (timer != null) {
      timer.cancel();
      timer=null;
    }
    scheduleTimerTaskForUnregistration();
    this.pMemMBQuantiles.stop();
    this.cpuCoreUsagePercentQuantiles.stop();
  }
}
