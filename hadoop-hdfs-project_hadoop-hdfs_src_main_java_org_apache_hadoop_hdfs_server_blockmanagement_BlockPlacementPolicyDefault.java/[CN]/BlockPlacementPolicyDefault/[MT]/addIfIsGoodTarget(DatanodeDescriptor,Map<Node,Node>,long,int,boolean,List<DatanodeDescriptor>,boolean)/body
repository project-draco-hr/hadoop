{
  if (isGoodTarget(node,blockSize,maxNodesPerRack,considerLoad,results,avoidStaleNodes)) {
    results.add(node);
    return addToExcludedNodes(node,excludedNodes);
  }
 else {
    return -1;
  }
}
