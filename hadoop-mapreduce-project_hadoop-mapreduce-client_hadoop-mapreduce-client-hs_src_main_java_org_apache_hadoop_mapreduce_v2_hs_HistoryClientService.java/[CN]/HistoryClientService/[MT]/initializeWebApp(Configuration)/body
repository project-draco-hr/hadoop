{
  webApp=new HsWebApp(history);
  String bindAddress=conf.get(JHAdminConfig.MR_HISTORY_WEBAPP_ADDRESS,JHAdminConfig.DEFAULT_MR_HISTORY_WEBAPP_ADDRESS);
  WebApps.$for("jobhistory",this).at(bindAddress).start(webApp);
}
