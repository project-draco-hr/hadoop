{
  super();
  this.original=path;
  this.qualified=original.makeQualified(fs.getUri(),fs.getWorkingDirectory());
  this.path=new Path(qualified.toUri().getPath());
  this.blockSize=blockSize;
  this.dereference=deref;
}
