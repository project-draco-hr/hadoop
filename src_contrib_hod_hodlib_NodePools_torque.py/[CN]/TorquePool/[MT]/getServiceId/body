def getServiceId(self):
    id = None
    nodeSets = self.nodeSetDict.values()
    if len(nodeSets):
        id = nodeSets[0].qsubId
    if (id == None):
        id = os.getenv('PBS_JOBID')
    return id
