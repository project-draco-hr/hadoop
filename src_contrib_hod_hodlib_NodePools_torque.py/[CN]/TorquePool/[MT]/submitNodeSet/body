def submitNodeSet(self, nodeSet, walltime=None, qosLevel=None, account=None):
    (argList, stdinList) = self.__gen_submit_params(nodeSet, walltime, qosLevel, account)
    (jobId, exitCode) = self.__torque.qsub(argList, stdinList)
    nodeSet.qsubId = jobId
    return (jobId, exitCode)
