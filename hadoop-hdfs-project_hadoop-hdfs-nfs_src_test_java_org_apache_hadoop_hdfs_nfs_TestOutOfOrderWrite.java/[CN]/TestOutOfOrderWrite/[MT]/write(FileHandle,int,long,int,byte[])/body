{
  XDR request=new XDR();
  RpcCall.write(request,xid,Nfs3Constant.PROGRAM,Nfs3Constant.VERSION,Nfs3Constant.NFSPROC3.WRITE.getValue());
  request.writeInt(0);
  request.writeInt(0);
  request.writeInt(0);
  request.writeInt(0);
  WRITE3Request write1=new WRITE3Request(handle,offset,count,WriteStableHow.UNSTABLE,ByteBuffer.wrap(data));
  write1.serialize(request);
  return request;
}
