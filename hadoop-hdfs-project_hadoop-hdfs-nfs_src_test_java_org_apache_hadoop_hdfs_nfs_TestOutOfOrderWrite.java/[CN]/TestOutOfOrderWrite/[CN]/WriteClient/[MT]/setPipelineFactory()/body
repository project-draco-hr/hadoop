{
  this.pipelineFactory=new ChannelPipelineFactory(){
    public ChannelPipeline getPipeline(){
      return Channels.pipeline(new RpcFrameDecoder(),new WriteHandler(request));
    }
  }
;
  return this.pipelineFactory;
}
