{
synchronized (this) {
    if (shutdown)     return;
    shutdown=true;
    this.notifyAll();
  }
  try {
    if (this.isAlive()) {
      this.join(60000);
    }
  }
 catch (  InterruptedException e) {
    Thread.currentThread().interrupt();
  }
}
