{
  Preconditions.checkArgument(namesystem.hasWriteLock());
  lock.lock();
  try {
    if (shutdown)     return;
    shutdown=true;
    doRescan.signalAll();
    scanFinished.signalAll();
  }
  finally {
    lock.unlock();
  }
}
