{
  if (i >= 0) {
    throw new AssertionError("Element already exists: element=" + element + ", created="+ created);
  }
  if (created == null) {
    created=new ArrayList<E>(DEFAULT_ARRAY_INITIAL_CAPACITY);
  }
  created.add(-i - 1,element);
}
