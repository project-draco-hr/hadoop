{
  final List<E> current=new ArrayList<E>(previous);
  for (  E d : dlist) {
    current.remove(d);
  }
  for (  E c : clist) {
    final int i=search(current,c.getKey());
    current.add(-i - 1,c);
  }
  return current;
}
