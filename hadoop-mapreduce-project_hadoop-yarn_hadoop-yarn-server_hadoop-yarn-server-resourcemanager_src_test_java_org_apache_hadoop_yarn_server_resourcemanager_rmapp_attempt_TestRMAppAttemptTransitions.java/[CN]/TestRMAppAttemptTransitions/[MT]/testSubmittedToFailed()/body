{
  submitApplicationAttempt();
  String message="Rejected";
  applicationAttempt.handle(new RMAppAttemptRejectedEvent(applicationAttempt.getAppAttemptId(),message));
  testAppAttemptSubmittedToFailedState(message);
}
