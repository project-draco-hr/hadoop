{
  String msg=super.getMessage();
  if (msg == null) {
    return "The NameSpace quota (directories and files)" + (pathName == null ? "" : (" of directory " + pathName)) + " is exceeded: quota="+ quota+ " file count="+ count;
  }
 else {
    return msg;
  }
}
