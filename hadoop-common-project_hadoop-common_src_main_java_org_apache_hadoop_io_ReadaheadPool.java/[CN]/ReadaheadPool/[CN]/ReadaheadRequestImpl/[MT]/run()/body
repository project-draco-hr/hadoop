{
  if (canceled)   return;
  try {
    NativeIO.POSIX.posixFadviseIfPossible(identifier,fd,off,len,NativeIO.POSIX.POSIX_FADV_WILLNEED);
  }
 catch (  IOException ioe) {
    if (canceled) {
      return;
    }
    LOG.warn("Failed readahead on " + identifier,ioe);
  }
}
