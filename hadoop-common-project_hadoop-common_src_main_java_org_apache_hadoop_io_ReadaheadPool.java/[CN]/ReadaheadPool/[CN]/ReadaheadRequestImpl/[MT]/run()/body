{
  if (canceled)   return;
  try {
    NativeIO.posixFadviseIfPossible(fd,off,len,NativeIO.POSIX_FADV_WILLNEED);
  }
 catch (  IOException ioe) {
    if (canceled) {
      return;
    }
    LOG.warn("Failed readahead on " + identifier,ioe);
  }
}
