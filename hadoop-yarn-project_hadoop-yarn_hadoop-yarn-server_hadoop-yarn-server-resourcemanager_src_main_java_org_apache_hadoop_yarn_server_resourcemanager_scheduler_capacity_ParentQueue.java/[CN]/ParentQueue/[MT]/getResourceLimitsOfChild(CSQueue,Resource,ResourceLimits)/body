{
  Resource myCurrentLimit=getCurrentResourceLimit(clusterResource,myLimits);
  Resource myMaxAvailableResource=Resources.subtract(myCurrentLimit,getUsedResources());
  Resource childLimit=Resources.add(myMaxAvailableResource,child.getUsedResources());
  return new ResourceLimits(childLimit);
}
