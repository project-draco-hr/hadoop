{
  if (!queueProperties.containsKey(queue))   throw new IllegalArgumentException("Invalid queuename " + queue);
  if (queueProperties.get(queue) != null) {
    return queueProperties.get(queue).getProperty(property);
  }
  return null;
}
