{
  final INode[] array=diff.accessPrevious(name);
  if (array != null) {
    return array[0];
  }
 else   if (!checkPosterior) {
    return null;
  }
 else {
    return posteriorDiff != null ? posteriorDiff.getChild(name,true) : INodeDirectoryWithSnapshot.this.getChild(name,null);
  }
}
