{
  final INode i=diff.accessPrevious(name,INode.DUMMY);
  if (i != INode.DUMMY) {
    return i;
  }
 else {
    return posteriorDiff != null ? posteriorDiff.getChild(name) : INodeDirectoryWithSnapshot.this.getChild(name,null);
  }
}
