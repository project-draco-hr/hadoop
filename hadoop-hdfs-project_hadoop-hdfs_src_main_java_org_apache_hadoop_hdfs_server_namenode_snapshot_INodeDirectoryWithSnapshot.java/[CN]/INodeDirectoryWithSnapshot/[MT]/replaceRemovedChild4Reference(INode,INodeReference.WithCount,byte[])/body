{
  final INodeReference.WithName ref=new INodeReference.WithName(this,newChild,childName);
  newChild.incrementReferenceCount();
  diffs.replaceChild(ListType.CREATED,oldChild,ref);
  Preconditions.checkState(diffs.replaceChild(ListType.DELETED,oldChild,ref));
  return ref;
}
