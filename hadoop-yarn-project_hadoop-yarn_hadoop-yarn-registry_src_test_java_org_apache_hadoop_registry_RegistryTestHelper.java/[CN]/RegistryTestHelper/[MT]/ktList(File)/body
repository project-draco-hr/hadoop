{
  if (!Shell.WINDOWS) {
    String path=keytab.getAbsolutePath();
    String out=Shell.execCommand(KTUTIL,"--keytab=" + path,"list","--keys");
    LOG.info("Listing of keytab {}:\n{}\n",path,out);
    return out;
  }
  return "";
}
