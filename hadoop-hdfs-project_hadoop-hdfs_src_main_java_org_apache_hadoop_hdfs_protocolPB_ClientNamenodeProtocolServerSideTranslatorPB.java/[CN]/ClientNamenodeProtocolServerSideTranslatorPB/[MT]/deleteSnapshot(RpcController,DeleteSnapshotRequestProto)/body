{
  try {
    server.deleteSnapshot(request.getSnapshotRoot(),request.getSnapshotName());
    return VOID_DELETE_SNAPSHOT_RESPONSE;
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
}
