{
  try {
    server.deleteSnapshot(request.getSnapshotName(),request.getSnapshotRoot());
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
  return VOID_DELETE_SNAPSHOT_RESPONSE;
}
