{
  try {
    return AddPathBasedCacheDirectiveResponseProto.newBuilder().setId(server.addPathBasedCacheDirective(PBHelper.convert(request.getInfo()))).build();
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
}
