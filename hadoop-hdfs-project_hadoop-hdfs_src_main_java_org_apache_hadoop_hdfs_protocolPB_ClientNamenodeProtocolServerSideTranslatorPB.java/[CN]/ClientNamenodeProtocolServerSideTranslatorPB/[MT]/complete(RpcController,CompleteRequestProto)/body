{
  try {
    return CompleteResponseProto.newBuilder().setResult(server.complete(req.getSrc(),req.getClientName(),PBHelper.convert(req.getLast()))).build();
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
}
