{
  try {
    server.createSnapshot(request.getSnapshotRoot(),request.getSnapshotName());
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
  return VOID_CREATE_SNAPSHOT_RESPONSE;
}
