{
  try {
    ContentSummaryProto result=PBHelper.convert(server.getContentSummary(req.getPath()));
    return GetContentSummaryResponseProto.newBuilder().setSummary(result).build();
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
}
