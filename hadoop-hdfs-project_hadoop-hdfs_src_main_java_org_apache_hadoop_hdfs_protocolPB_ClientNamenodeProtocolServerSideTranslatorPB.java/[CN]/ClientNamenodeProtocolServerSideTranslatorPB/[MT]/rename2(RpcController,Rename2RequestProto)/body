{
  try {
    server.rename2(req.getSrc(),req.getDst(),req.getOverwriteDest() ? Rename.OVERWRITE : Rename.NONE);
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
  return VOID_RENAME2_RESPONSE;
}
