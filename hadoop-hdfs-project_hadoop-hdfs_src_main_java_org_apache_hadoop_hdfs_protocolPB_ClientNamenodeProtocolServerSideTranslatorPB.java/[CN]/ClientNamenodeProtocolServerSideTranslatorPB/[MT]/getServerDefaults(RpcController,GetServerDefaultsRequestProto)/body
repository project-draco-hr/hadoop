{
  try {
    return GetServerDefaultsResponseProto.newBuilder().setServerDefaults(PBHelper.convert(server.getServerDefaults())).build();
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
}
