{
  try {
    server.removePathBasedCacheDirective(request.getId());
    return RemovePathBasedCacheDirectiveResponseProto.newBuilder().build();
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
}
