{
  try {
    DirectoryListingProto result=PBHelper.convert(server.getListing(req.getSrc(),req.getStartAfter().toByteArray(),req.getNeedLocation()));
    return GetListingResponseProto.newBuilder().setDirList(result).build();
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
}
