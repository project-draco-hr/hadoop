{
  if (verifyChecksum) {
    if (storageType != null && storageType.isTransient()) {
      return true;
    }
 else {
      return replica.addNoChecksumAnchor();
    }
  }
 else {
    return true;
  }
}
