{
  conf=new Configuration();
  conf.setLong("dfs.blocksize",100 * 1024);
  conf.set(HdfsClientConfigKeys.DFS_CLIENT_HTRACE_PREFIX + SpanReceiverHost.SPAN_RECEIVERS_CONF_SUFFIX,SetSpanReceiver.class.getName());
}
