{
  return new AdminService(getConfig(),scheduler,getRMContext(),this.nodesListManager,clientRMService,applicationMasterService,resourceTrackerService){
    @Override protected void serviceStart(){
    }
    @Override protected void serviceStop(){
    }
  }
;
}
