{
  WebResource r=resource();
  JSONObject json=r.path("ws").path("v1").path("cluster").path("scheduler").accept(MediaType.APPLICATION_JSON).get(JSONObject.class);
  verifyClusterSchedulerFifo(json);
}
