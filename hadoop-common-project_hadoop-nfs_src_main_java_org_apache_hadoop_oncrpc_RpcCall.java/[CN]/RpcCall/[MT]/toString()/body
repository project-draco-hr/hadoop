{
  return String.format("Xid:%d, messageType:%d, rpcVersion:%d, program:%d," + " version:%d, procedure:%d, credential:%s, verifier:%s",xid,messageType,rpcVersion,program,version,procedure,credential.toString(),verifier.toString());
}
