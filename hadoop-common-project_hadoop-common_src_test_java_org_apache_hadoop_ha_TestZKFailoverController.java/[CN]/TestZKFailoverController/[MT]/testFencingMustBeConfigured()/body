{
  svc1=Mockito.spy(svc1);
  Mockito.doThrow(new BadFencingConfigurationException("no fencing")).when(svc1).checkFencingConfigured();
  assertEquals(0,runFC(svc1,"-formatZK"));
  assertEquals(ZKFailoverController.ERR_CODE_NO_FENCER,runFC(svc1));
}
