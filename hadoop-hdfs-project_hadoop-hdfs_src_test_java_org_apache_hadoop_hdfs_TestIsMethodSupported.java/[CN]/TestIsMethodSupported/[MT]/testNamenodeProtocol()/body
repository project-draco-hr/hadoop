{
  NamenodeProtocolTranslatorPB translator=new NamenodeProtocolTranslatorPB(nnAddress,conf,UserGroupInformation.getCurrentUser());
  boolean exists=translator.isMethodSupported("rollEditLog");
  Assert.assertTrue(exists);
  exists=translator.isMethodSupported("bogusMethod");
  Assert.assertFalse(exists);
}
