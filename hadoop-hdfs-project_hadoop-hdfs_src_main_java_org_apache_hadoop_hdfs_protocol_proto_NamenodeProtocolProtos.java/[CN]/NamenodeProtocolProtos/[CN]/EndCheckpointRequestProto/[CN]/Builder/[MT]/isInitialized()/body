{
  if (!hasRegistration()) {
    return false;
  }
  if (!hasSignature()) {
    return false;
  }
  if (!getRegistration().isInitialized()) {
    return false;
  }
  if (!getSignature().isInitialized()) {
    return false;
  }
  return true;
}
