{
  String msg=super.getMessage();
  if (msg != null) {
    return msg;
  }
  String myMsg="Unresolved path " + originalPath;
  try {
    return getResolvedPath().toString();
  }
 catch (  IOException e) {
  }
  return myMsg;
}
