{
  long readahead=cachingStrategy.getReadahead() != null ? cachingStrategy.getReadahead() : DFSConfigKeys.DFS_DATANODE_READAHEAD_BYTES_DEFAULT;
  this.maxReadahead=(int)Math.min(Integer.MAX_VALUE,readahead);
  return this;
}
