{
  Injector injector=WebAppTests.createMockInjector(RMContext.class,mockRMContext(3,1,2,8 * GiB),new Module(){
    @Override public void configure(    Binder binder){
      try {
        binder.bind(ResourceManager.class).toInstance(mockRm(3,1,2,8 * GiB));
      }
 catch (      IOException e) {
        throw new IllegalStateException(e);
      }
    }
  }
);
  injector.getInstance(RmView.class).render();
  WebAppTests.flushOutput(injector);
}
