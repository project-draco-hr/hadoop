{
  String fileid="putimage=1&port=" + imagePort + "&machine="+ infoBindAddress+ "&token="+ sig.toString()+ "&newChecksum="+ checkpointImage.getStorage().getImageDigest();
  LOG.info("Posted URL " + fsName + fileid);
  TransferFsImage.getFileClient(fsName,fileid,(File[])null,false);
}
