{
  int seconds=5;
  if (args.length >= 1) {
    try {
      seconds=Integer.valueOf(args[0]);
    }
 catch (    NumberFormatException e) {
    }
  }
  DelayEchoApp app=new DelayEchoApp();
  app.go(seconds);
}
