{
  int memory=Math.max(ask.getCapability().getMemory(),minMemory);
  int normalizedMemory=minMemory * ((memory / minMemory) + (memory % minMemory > 0 ? 1 : 0));
  ask.getCapability().setMemory(Math.min(normalizedMemory,maxMemory));
}
