{
  this.block=block;
  this.sources=sources;
  this.targets=DatanodeStorageInfo.toDatanodeInfos(targetDnStorageInfo);
  this.targetStorageIDs=DatanodeStorageInfo.toStorageIDs(targetDnStorageInfo);
  this.targetStorageTypes=DatanodeStorageInfo.toStorageTypes(targetDnStorageInfo);
  this.liveBlockIndices=liveBlockIndices;
}
