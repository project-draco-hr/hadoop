{
  if (role == PriorityAuthorization.ADMIN) {
    return;
  }
  if (role == PriorityAuthorization.USER && request.getParameter("user").equals(queue)) {
    return;
  }
  throw new IOException("ACCESS DENIED: " + query);
}
