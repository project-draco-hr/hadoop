{
  if (lastEvent != null && event.getTimeStamp() < lastEvent.getTimeStamp())   throw new IllegalArgumentException("Event happens in the past: " + event.getClass());
  event.setInternalCount(eventCount++);
  return events.add(event);
}
