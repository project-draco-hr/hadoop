{
  ContainerLaunchContext container=applicationMasterContext.getAMContainerSpec();
  LOG.info("Command to launch container " + containerID + " : "+ StringUtils.arrayToString(container.getCommands().toArray(new String[0])));
  container.setUser(applicationMasterContext.getAMContainerSpec().getUser());
  setupTokensAndEnv(container,containerID);
  return container;
}
