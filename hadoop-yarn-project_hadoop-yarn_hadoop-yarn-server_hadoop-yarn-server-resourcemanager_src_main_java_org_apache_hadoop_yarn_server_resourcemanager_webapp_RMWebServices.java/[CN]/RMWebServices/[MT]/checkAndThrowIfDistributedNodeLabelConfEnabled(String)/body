{
  if (isDistributedNodeLabelConfiguration) {
    String msg=String.format("Error when invoke method=%s because of " + "distributed node label configuration enabled.",operation);
    LOG.error(msg);
    throw new IOException(msg);
  }
}
