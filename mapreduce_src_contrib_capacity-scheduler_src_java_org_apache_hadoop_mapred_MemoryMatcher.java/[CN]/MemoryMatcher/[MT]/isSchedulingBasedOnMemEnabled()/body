{
  if (getLimitMaxMemForMapSlot() == JobConf.DISABLED_MEMORY_LIMIT || getLimitMaxMemForReduceSlot() == JobConf.DISABLED_MEMORY_LIMIT || getMemSizeForMapSlot() == JobConf.DISABLED_MEMORY_LIMIT || getMemSizeForReduceSlot() == JobConf.DISABLED_MEMORY_LIMIT) {
    return false;
  }
  return true;
}
