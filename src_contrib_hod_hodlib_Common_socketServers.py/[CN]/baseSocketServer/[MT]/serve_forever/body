def serve_forever(self):
    'Handle requests until stopForever event flag indicates stop.'
    self.mThread = threading.Thread(name='baseSocketServer', target=self._serve_forever)
    self.mThread.start()
    return self.mThread
