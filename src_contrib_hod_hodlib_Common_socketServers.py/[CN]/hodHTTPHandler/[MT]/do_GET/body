def do_GET(self):
    self.fullUrl = ('http://%s:%s%s' % (self.server.server_address[0], self.server.server_address[1], self.path))
    parsedUrl = urlparse(self.fullUrl)
    self.writeHeaders()
    self.writeData(parsedUrl)
