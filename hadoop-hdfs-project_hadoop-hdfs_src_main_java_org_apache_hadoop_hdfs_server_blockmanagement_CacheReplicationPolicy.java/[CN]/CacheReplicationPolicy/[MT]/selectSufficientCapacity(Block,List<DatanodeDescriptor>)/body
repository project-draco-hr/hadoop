{
  List<DatanodeDescriptor> sufficient=new ArrayList<DatanodeDescriptor>(targets.size());
  for (  DatanodeDescriptor dn : targets) {
    long remaining=dn.getCacheRemaining();
    if (remaining >= block.getNumBytes()) {
      sufficient.add(dn);
    }
  }
  return sufficient;
}
