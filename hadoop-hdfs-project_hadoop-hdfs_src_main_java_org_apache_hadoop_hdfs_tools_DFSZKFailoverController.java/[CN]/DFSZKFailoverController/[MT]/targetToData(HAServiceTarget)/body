{
  InetSocketAddress addr=target.getAddress();
  return ActiveNodeInfo.newBuilder().setHostname(addr.getHostName()).setPort(addr.getPort()).setNameserviceId(localTarget.getNameServiceId()).setNamenodeId(localTarget.getNameNodeId()).build().toByteArray();
}
