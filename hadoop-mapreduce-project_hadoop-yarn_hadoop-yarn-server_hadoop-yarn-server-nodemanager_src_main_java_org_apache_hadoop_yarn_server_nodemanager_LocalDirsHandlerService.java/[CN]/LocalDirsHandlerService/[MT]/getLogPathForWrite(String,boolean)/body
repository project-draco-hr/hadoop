{
  Configuration conf=getConfig();
  Path path=null;
synchronized (conf) {
    path=logDirsAllocator.getLocalPathForWrite(pathStr,LocalDirAllocator.SIZE_UNKNOWN,conf,checkWrite);
  }
  return path;
}
