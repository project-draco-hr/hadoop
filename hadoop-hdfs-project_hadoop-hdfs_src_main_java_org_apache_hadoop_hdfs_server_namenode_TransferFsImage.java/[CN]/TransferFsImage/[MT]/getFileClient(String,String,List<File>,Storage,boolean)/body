{
  String str=HttpConfig.getSchemePrefix() + nnHostPort + "/getimage?"+ queryString;
  LOG.info("Opening connection to " + str);
  URL url=new URL(str);
  return doGetUrl(url,localPaths,dstStorage,getChecksum);
}
