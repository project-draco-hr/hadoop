{
  for (int i=0; i < responseTimeCountInLastWindow.length(); i++) {
    rb.addGauge(Interns.info("Priority." + i + ".CallVolume","Call volume " + "of priority " + i),responseTimeCountInLastWindow.get(i));
  }
}
