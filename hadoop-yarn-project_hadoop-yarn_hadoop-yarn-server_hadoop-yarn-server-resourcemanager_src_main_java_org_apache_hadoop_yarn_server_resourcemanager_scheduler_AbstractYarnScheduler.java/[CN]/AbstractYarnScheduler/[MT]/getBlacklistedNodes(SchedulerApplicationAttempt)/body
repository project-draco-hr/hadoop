{
  NodeFilter nodeFilter=new NodeFilter(){
    @Override public boolean accept(    SchedulerNode node){
      return SchedulerAppUtils.isBlacklisted(app,node,LOG);
    }
  }
;
  return nodeTracker.getNodes(nodeFilter);
}
