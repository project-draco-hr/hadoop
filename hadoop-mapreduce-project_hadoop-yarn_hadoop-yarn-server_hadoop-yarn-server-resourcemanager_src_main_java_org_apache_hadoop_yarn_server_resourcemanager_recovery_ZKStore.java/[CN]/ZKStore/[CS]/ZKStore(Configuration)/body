{
  this.conf=conf;
  this.ZK_ADDRESS=conf.get(RMConfig.ZK_ADDRESS);
  this.ZK_TIMEOUT=conf.getInt(RMConfig.ZK_SESSION_TIMEOUT,RMConfig.DEFAULT_ZK_TIMEOUT);
  zkClient=new ZooKeeper(this.ZK_ADDRESS,this.ZK_TIMEOUT,createZKWatcher());
}
