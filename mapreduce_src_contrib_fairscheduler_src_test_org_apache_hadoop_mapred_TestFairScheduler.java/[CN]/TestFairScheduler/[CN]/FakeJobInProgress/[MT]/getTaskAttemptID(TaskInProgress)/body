{
  JobID jobId=getJobID();
  TaskType type=tip.isMapTask() ? TaskType.MAP : TaskType.REDUCE;
  return new TaskAttemptID(jobId.getJtIdentifier(),jobId.getId(),type,tip.getIdWithinJob(),tip.nextTaskId++);
}
