{
  return new TaskAttemptListenerImpl(getContext(),null){
    public void startRpcServer(){
    }
    public void stopRpcServer(){
    }
    public void init(    Configuration conf){
      conf.setInt("mapreduce.task.timeout",1 * 1000);
      super.init(conf);
    }
  }
;
}
