{
  super.computeContentSummary(countsMap);
  countsMap.getCounts(Key.SNAPSHOT).add(Content.SNAPSHOT,snapshotsByNames.size());
  countsMap.getCounts(Key.CURRENT).add(Content.SNAPSHOTTABLE_DIRECTORY,1);
  return countsMap;
}
