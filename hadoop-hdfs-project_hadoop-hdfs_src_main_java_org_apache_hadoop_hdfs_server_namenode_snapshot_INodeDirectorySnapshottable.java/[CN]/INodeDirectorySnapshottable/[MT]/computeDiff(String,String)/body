{
  Snapshot fromSnapshot=getSnapshotByName(from);
  Snapshot toSnapshot=getSnapshotByName(to);
  SnapshotDiffReport diffs=new SnapshotDiffReport(this,fromSnapshot,toSnapshot);
  computeDiffInDir(this,diffs);
  return diffs;
}
