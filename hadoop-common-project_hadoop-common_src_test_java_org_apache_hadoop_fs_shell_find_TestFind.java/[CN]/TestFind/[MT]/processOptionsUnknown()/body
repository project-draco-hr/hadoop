{
  Find find=new Find();
  find.setConf(conf);
  String args="path -unknown";
  try {
    find.processOptions(getArgs(args));
    fail("Unknown expression not caught");
  }
 catch (  IOException e) {
  }
}
