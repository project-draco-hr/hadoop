{
  if (config == null) {
    return;
  }
  for (  Map.Entry<String,String> entry : config.entrySet()) {
    EntityColumnFamily.CONFIG.store(rowKey,entityTable,entry.getKey(),entry.getValue());
  }
}
