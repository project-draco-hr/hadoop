{
  MockTaskAttemptImpl attempt=new MockTaskAttemptImpl(getID(),++taskAttemptCounter,eventHandler,taskAttemptListener,remoteJobConfFile,partition,conf,committer,jobToken,fsTokens,clock,appContext);
  taskAttempts.add(attempt);
  return attempt;
}
