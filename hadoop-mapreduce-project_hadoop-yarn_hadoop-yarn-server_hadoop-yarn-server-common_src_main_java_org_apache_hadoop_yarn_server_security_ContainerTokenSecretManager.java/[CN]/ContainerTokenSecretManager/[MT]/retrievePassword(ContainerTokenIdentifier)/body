{
  LOG.debug("Retrieving password for " + identifier.getContainerID() + " to be run on NM "+ identifier.getNmHostName());
  return createPassword(identifier.getBytes(),this.secretkeys.get(identifier.getNmHostName()));
}
