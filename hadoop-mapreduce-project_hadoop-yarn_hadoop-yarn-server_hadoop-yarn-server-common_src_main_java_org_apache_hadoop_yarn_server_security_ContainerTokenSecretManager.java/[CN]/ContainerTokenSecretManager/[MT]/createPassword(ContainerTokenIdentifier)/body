{
  LOG.debug("Creating password for " + identifier.getContainerID() + " to be run on NM "+ identifier.getNmHostName()+ " "+ this.secretkeys.get(identifier.getNmHostName()));
  return createPassword(identifier.getBytes(),this.secretkeys.get(identifier.getNmHostName()));
}
