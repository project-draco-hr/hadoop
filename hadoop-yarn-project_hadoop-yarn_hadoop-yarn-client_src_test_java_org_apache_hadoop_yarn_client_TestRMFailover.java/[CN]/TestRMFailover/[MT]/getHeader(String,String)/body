{
  String fieldHeader=null;
  try {
    Map<String,List<String>> map=new URL(url).openConnection().getHeaderFields();
    fieldHeader=map.get(field).get(0);
  }
 catch (  Exception e) {
  }
  return fieldHeader;
}
