def test_read_file_url(self):
    file = tempfile.NamedTemporaryFile()
    file.write('Contents go here')
    file.flush()
    self.assertEqual('Contents go here', InstanceUserData(('file://%s' % file.name), {}).read())
    file.close()
