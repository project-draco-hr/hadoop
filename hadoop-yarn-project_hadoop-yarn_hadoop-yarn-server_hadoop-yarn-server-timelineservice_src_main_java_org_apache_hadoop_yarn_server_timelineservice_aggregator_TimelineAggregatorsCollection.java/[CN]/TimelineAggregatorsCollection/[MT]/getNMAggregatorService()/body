{
  Configuration conf=getConfig();
  final YarnRPC rpc=YarnRPC.create(conf);
  return (AggregatorNodemanagerProtocol)rpc.getProxy(AggregatorNodemanagerProtocol.class,nmAggregatorServiceAddress,conf);
}
