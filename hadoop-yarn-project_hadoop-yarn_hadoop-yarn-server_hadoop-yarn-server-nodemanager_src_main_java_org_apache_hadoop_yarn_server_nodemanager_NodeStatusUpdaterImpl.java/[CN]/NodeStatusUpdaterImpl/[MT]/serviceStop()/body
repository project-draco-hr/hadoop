{
  if (this.registeredWithRM && !this.isStopped && !isNMUnderSupervisionWithRecoveryEnabled()&& !context.getDecommissioned()) {
    unRegisterNM();
  }
  this.isStopped=true;
  stopRMProxy();
  super.serviceStop();
}
