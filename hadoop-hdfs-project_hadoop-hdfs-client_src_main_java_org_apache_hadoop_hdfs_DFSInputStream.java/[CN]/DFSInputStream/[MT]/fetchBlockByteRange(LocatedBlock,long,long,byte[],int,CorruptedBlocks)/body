{
  block=refreshLocatedBlock(block);
  while (true) {
    DNAddrPair addressPair=chooseDataNode(block,null);
    try {
      actualGetFromOneDataNode(addressPair,block,start,end,buf,offset,corruptedBlocks);
      return;
    }
 catch (    IOException e) {
    }
  }
}
