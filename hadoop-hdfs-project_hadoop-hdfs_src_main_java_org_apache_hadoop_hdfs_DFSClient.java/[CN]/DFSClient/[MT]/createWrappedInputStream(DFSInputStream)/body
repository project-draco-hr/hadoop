{
  final FileEncryptionInfo feInfo=dfsis.getFileEncryptionInfo();
  if (feInfo != null) {
    KeyVersion decrypted=decryptEncryptedDataEncryptionKey(feInfo);
    CryptoCodec codec=CryptoCodec.getInstance(conf,feInfo.getCipherSuite());
    Preconditions.checkNotNull(codec == null,"No crypto codec classes with cipher suite configured.");
    final CryptoInputStream cryptoIn=new CryptoInputStream(dfsis,codec,decrypted.getMaterial(),feInfo.getIV());
    return new HdfsDataInputStream(cryptoIn);
  }
 else {
    return new HdfsDataInputStream(dfsis);
  }
}
