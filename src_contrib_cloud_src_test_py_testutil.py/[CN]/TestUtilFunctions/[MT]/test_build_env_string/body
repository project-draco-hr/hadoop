def test_build_env_string(self):
    self.assertEqual('', build_env_string())
    self.assertEqual("a='b' c='d'", build_env_string(env_strings=['a=b', 'c=d']))
    self.assertEqual("a='b' c='d'", build_env_string(pairs={'a': 'b', 'c': 'd', }))
