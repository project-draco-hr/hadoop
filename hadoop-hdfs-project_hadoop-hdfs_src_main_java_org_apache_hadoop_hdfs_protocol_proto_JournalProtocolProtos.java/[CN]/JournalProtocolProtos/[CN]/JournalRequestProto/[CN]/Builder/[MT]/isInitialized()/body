{
  if (!hasRegistration()) {
    return false;
  }
  if (!hasFirstTxnId()) {
    return false;
  }
  if (!hasNumTxns()) {
    return false;
  }
  if (!hasRecords()) {
    return false;
  }
  if (!getRegistration().isInitialized()) {
    return false;
  }
  return true;
}
