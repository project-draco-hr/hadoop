{
  if (request.getApplicationId().getId() == 1) {
    return GetTimelineCollectorContextResponse.newInstance("test_user_id","test_flow_id","test_flow_run_id");
  }
 else {
    throw new YarnException("The application is not found.");
  }
}
