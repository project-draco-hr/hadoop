{
  return new LogAggregationService(new AsyncDispatcher(),context,deletionService){
    @Override public void handle(    LogAggregatorEvent event){
switch (event.getType()) {
case APPLICATION_STARTED:
        break;
case CONTAINER_FINISHED:
      break;
case APPLICATION_FINISHED:
    break;
default :
}
}
}
;
}
