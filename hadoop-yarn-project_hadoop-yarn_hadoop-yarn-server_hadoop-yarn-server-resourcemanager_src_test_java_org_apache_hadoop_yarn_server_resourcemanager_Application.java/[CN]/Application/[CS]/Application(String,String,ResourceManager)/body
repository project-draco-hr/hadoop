{
  this.user=user;
  this.queue=queue;
  this.resourceManager=resourceManager;
  this.applicationId=this.resourceManager.getClientRMService().getNewApplicationId();
  this.applicationAttemptId=ApplicationAttemptId.newInstance(this.applicationId,this.numAttempts.getAndIncrement());
}
