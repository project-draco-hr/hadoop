{
  Resource currentSpec=requestSpec.put(schedulerKey,capability);
  if (currentSpec != null) {
    throw new IllegalStateException("Resource spec already exists for " + "priority " + schedulerKey.getPriority().getPriority() + " - "+ currentSpec.getMemorySize());
  }
}
