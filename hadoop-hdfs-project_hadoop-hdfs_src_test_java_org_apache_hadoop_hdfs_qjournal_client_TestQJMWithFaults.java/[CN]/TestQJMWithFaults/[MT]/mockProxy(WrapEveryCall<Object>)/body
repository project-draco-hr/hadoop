{
  QJournalProtocol mock=Mockito.mock(QJournalProtocol.class,Mockito.withSettings().defaultAnswer(wrapper).extraInterfaces(Closeable.class));
  Mockito.doNothing().when((Closeable)mock).close();
  return mock;
}
