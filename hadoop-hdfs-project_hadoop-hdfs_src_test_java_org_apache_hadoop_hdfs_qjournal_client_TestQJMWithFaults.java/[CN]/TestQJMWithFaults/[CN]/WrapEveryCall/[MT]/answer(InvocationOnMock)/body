{
  if (!Closeable.class.equals(invocation.getMethod().getDeclaringClass())) {
    beforeCall(invocation);
  }
  try {
    return (T)invocation.getMethod().invoke(realObj,invocation.getArguments());
  }
 catch (  InvocationTargetException ite) {
    throw ite.getCause();
  }
}
