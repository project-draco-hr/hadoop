{
  if (children == null) {
    children=new ArrayList<AbstractQueue>();
  }
  if (children.contains(queue)) {
    LOG.warn(" The queue " + queue.getName() + " already "+ "exists hence ignoring  the current value ");
    return;
  }
  this.children.add(queue);
}
