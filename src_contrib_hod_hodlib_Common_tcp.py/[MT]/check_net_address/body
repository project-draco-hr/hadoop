def check_net_address(address):
    valid = True
    pieces = string.split(address, '.')
    if (len(pieces) != 4):
        valid = False
    else:
        for piece in pieces:
            if ((int(piece) < 0) or (int(piece) > 255)):
                valid = False
    return valid
