{
  String doneDirPrefix=conf.get(JHConfig.HISTORY_DONE_DIR_KEY);
  if (doneDirPrefix == null) {
    doneDirPrefix=conf.get(MRConstants.APPS_STAGING_DIR_KEY) + "/history/done";
  }
  return doneDirPrefix;
}
