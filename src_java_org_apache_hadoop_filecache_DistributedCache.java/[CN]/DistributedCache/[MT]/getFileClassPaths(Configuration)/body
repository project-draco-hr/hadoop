{
  ArrayList<String> list=(ArrayList<String>)conf.getStringCollection("mapred.job.classpath.files");
  if (list.size() == 0) {
    return null;
  }
  Path[] paths=new Path[list.size()];
  for (int i=0; i < list.size(); i++) {
    paths[i]=new Path(list.get(i));
  }
  return paths;
}
