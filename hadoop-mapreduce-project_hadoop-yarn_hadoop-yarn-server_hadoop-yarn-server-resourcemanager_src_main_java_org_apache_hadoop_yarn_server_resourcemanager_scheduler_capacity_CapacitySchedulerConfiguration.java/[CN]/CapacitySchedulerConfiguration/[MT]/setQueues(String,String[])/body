{
  set(getQueuePrefix(queue) + QUEUES,StringUtils.arrayToString(subQueues));
  LOG.info("CSConf - setQueues: qPrefix=" + getQueuePrefix(queue) + ", queues="+ StringUtils.arrayToString(subQueues));
}
