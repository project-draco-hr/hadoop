{
  return createWriter(conf,Writer.stream(out),Writer.keyClass(keyClass),Writer.valueClass(valClass),Writer.compressionType(compressionType),Writer.compressionCodec(codec),Writer.metadata(metadata));
}
