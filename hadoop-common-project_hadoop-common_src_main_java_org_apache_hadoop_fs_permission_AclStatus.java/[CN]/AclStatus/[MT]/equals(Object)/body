{
  if (o == null) {
    return false;
  }
  if (getClass() != o.getClass()) {
    return false;
  }
  AclStatus other=(AclStatus)o;
  return Objects.equal(file,other.file) && Objects.equal(owner,other.owner) && Objects.equal(group,other.group)&& Objects.equal(acl,other.acl);
}
