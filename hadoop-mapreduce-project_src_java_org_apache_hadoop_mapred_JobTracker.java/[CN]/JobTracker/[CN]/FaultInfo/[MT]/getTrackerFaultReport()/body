{
  StringBuffer sb=new StringBuffer();
  for (  String reasons : rfbMap.values()) {
    sb.append(reasons);
    sb.append("\n");
  }
  if (sb.length() > 0) {
    sb.replace(sb.length() - 1,sb.length(),"");
  }
  return sb.toString();
}
