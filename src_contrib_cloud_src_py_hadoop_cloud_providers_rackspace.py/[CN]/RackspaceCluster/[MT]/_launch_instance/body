def _launch_instance(self, roles, image_id, size_id, metadata, files):
    instance_name = ('%s-%s' % (self.name, uuid.uuid4().hex[(-8):]))
    node = self.driver.create_node(instance_name, self._find_image(image_id), self._find_size(size_id), metadata=metadata, files=files)
    return node
