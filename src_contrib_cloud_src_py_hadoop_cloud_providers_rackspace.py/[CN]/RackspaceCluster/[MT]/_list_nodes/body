@staticmethod
def _list_nodes(driver, retries=5):
    attempts = 0
    while True:
        try:
            return driver.list_nodes()
        except IOError:
            attempts = (attempts + 1)
            if (attempts > retries):
                raise
            time.sleep(5)
