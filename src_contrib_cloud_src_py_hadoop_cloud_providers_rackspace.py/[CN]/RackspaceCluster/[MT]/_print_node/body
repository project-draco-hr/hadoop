def _print_node(self, node, out):
    out.write('\t'.join((node.extra['metadata']['roles'], node.id, node.name, self._ip_list_to_string(node.public_ip), self._ip_list_to_string(node.private_ip), STATE_MAP_REVERSED[node.state])))
    out.write('\n')
