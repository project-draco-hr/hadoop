{
  if (idx == -1) {
    makeRequest();
  }
 else   if ((entries != null) && (idx >= entries.size())) {
    if (entries.size() < repliesPerRequest) {
      entries=null;
    }
 else {
      makeRequest();
    }
  }
}
