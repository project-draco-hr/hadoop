{
  for (  TaskType t : types) {
    TaskAttemptUnsuccessfulCompletionEvent tauce=new TaskAttemptUnsuccessfulCompletionEvent(id,t,state,0L,"",-1,"",NULL_SPLITS_ARRAY);
    assertEquals(expected,tauce.getEventType());
  }
}
