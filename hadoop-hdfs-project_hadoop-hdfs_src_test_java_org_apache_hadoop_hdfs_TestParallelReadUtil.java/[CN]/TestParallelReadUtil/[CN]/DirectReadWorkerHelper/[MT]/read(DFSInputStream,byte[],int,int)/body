{
  ByteBuffer bb=ByteBuffer.wrap(target);
  int cnt=0;
synchronized (dis) {
    dis.seek(startOff);
    while (cnt < len) {
      int read=dis.read(bb);
      if (read == -1) {
        return read;
      }
      cnt+=read;
    }
  }
  return cnt;
}
