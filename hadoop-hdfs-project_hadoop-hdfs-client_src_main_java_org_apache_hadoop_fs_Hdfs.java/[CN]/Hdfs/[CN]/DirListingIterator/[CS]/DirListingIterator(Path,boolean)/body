{
  this.src=Hdfs.this.getUriPath(p);
  this.needLocation=needLocation;
  thisListing=dfs.listPaths(src,HdfsFileStatus.EMPTY_NAME,needLocation);
  if (thisListing == null) {
    throw new FileNotFoundException("File " + src + " does not exist.");
  }
}
