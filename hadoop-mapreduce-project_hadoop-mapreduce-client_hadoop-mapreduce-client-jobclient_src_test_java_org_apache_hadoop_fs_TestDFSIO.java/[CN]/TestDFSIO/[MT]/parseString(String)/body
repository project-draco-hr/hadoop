{
  if (sMultiple == null || sMultiple.isEmpty())   return MB;
  String sMU=sMultiple.toUpperCase(Locale.ENGLISH);
  if (B.name().toUpperCase(Locale.ENGLISH).endsWith(sMU))   return B;
  if (KB.name().toUpperCase(Locale.ENGLISH).endsWith(sMU))   return KB;
  if (MB.name().toUpperCase(Locale.ENGLISH).endsWith(sMU))   return MB;
  if (GB.name().toUpperCase(Locale.ENGLISH).endsWith(sMU))   return GB;
  if (TB.name().toUpperCase(Locale.ENGLISH).endsWith(sMU))   return TB;
  throw new IllegalArgumentException("Unsupported ByteMultiple " + sMultiple);
}
