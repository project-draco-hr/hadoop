{
  final InetSocketAddress nnAddr=NetUtils.createSocketAddr(token.getService().toString());
  final URI uri=DFSUtil.createUri(WebHdfsFileSystem.SCHEME,nnAddr);
  return (WebHdfsFileSystem)FileSystem.get(uri,conf);
}
