{
  final HttpURLConnection conn;
  try {
    conn=(HttpURLConnection)connectionFactory.openConnection(url,op.getRequireAuth());
  }
 catch (  AuthenticationException e) {
    throw new IOException(e);
  }
  return conn;
}
