{
  String host=getProperty("HOST");
  if (host == null) {
    try {
      host=InetAddress.getLocalHost().getHostName();
    }
 catch (    IOException io) {
      io.printStackTrace();
    }
  }
  return host;
}
