{
  checkBlock(block);
  ReceivedDeletedBlockInfo bInfo=new ReceivedDeletedBlockInfo(block.getLocalBlock(),ReceivedDeletedBlockInfo.TODELETE_HINT);
  for (  BPServiceActor actor : bpServices) {
    actor.notifyNamenodeDeletedBlock(bInfo);
  }
}
