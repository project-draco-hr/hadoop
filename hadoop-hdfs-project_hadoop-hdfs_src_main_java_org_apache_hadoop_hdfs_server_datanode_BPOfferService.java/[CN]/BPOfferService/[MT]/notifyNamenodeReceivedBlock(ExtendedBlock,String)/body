{
  checkBlock(block);
  checkDelHint(delHint);
  ReceivedDeletedBlockInfo bInfo=new ReceivedDeletedBlockInfo(block.getLocalBlock(),delHint);
  for (  BPServiceActor actor : bpServices) {
    actor.notifyNamenodeReceivedBlock(bInfo);
  }
}
