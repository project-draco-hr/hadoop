{
  BlockStoragePolicy[] policies=dispatcher.getDistributedFileSystem().getStoragePolicySuite();
  for (  BlockStoragePolicy policy : policies) {
    this.blockStoragePolicies[policy.getId()]=policy;
  }
}
