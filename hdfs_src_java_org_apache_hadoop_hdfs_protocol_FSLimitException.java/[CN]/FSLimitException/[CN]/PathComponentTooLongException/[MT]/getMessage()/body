{
  Path violator=new Path(pathName);
  return "The maximum path component name limit of " + violator.getName() + " in directory "+ violator.getParent()+ " is exceeded: limit="+ quota+ " length="+ count;
}
