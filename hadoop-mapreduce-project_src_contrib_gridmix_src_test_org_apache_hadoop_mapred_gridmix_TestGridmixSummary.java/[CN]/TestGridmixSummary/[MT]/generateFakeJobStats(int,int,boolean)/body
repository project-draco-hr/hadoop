{
  Job fakeJob=new Job(){
    @Override public int getNumReduceTasks(){
      return numReds;
    }
    @Override public boolean isSuccessful() throws IOException, InterruptedException {
      return isSuccessful;
    }
  }
;
  return new JobStats(numMaps,fakeJob);
}
