{
  nodeLabel=nodeLabel.length() == 0 ? "<DEFAULT_PARTITION>" : nodeLabel;
  ResponseInfo ri=info("\'" + lqinfo.getQueuePath().substring(5) + "\' Queue Status for Partition \'"+ nodeLabel+ "\'");
  renderQueueCapacityInfo(ri);
  html._(InfoBlock.class);
  ri.clear();
  ri=info("\'" + lqinfo.getQueuePath().substring(5) + "\' Queue Status")._("Queue State:",lqinfo.getQueueState());
  renderCommonLeafQueueInfo(ri);
  html._(InfoBlock.class);
  ri.clear();
}
