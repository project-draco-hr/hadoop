{
  String user=callerUGI.getShortUserName();
  if (!areACLsEnabled()) {
    return true;
  }
  if (user.equals(applicationOwner) || acl.isUserAllowed(callerUGI)) {
    return true;
  }
  return false;
}
