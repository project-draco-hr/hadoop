{
  if (element == ImageElement.NUM_BYTES)   current.totalFileSize+=Long.parseLong(value);
 else   if (element == ImageElement.REPLICATION)   current.totalReplications+=Long.parseLong(value);
 else   if (element == ImageElement.INODE_PATH)   current.pathNames.add(value);
}
