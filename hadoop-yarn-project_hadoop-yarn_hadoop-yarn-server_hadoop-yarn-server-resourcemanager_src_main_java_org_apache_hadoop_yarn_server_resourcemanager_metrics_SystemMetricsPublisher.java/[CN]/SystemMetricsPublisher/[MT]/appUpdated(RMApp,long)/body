{
  if (publishSystemMetrics) {
    dispatcher.getEventHandler().handle(new ApplicationUpdatedEvent(app.getApplicationId(),app.getQueue(),updatedTime,app.getApplicationSubmissionContext().getPriority()));
  }
}
