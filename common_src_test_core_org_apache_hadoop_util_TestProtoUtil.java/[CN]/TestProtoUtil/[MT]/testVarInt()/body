{
  for (  int value : TEST_VINT_VALUES) {
    doVarIntTest(value);
  }
  for (int i=1; i != 0; i<<=1) {
    doVarIntTest(i);
    doVarIntTest(-i);
    doVarIntTest(i - 1);
    doVarIntTest(~i);
  }
}
