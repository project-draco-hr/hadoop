{
  TimelineAggregatorsCollection aggregatorsCollection=spy(new TimelineAggregatorsCollection());
  doReturn(new Configuration()).when(aggregatorsCollection).getConfig();
  PerNodeTimelineAggregatorsAuxService auxService=spy(new PerNodeTimelineAggregatorsAuxService(aggregatorsCollection));
  return auxService;
}
