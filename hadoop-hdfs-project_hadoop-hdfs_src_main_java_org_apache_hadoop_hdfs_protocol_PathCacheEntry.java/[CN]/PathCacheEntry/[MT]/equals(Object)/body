{
  try {
    PathCacheEntry other=(PathCacheEntry)o;
    return new EqualsBuilder().append(this.entryId,other.entryId).append(this.directive,other.directive).isEquals();
  }
 catch (  ClassCastException e) {
    return false;
  }
}
