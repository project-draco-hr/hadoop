{
  this.readLock.lock();
  try {
    return BuilderUtils.newContainerStatus(this.container.getId(),getCurrentState(),diagnostics.toString(),exitCode);
  }
  finally {
    this.readLock.unlock();
  }
}
