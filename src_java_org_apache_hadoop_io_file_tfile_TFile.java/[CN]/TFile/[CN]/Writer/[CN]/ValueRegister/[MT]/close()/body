{
  if (closed == true) {
    return;
  }
  try {
    ++errorCount;
    super.close();
    blkRecordCount++;
    tfileMeta.incRecordCount();
    finishDataBlock(false);
    --errorCount;
  }
  finally {
    closed=true;
    state=State.READY;
  }
}
