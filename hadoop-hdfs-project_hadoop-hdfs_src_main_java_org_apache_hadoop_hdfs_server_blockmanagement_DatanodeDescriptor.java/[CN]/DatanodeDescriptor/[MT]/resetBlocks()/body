{
  setCapacity(0);
  setRemaining(0);
  setBlockPoolUsed(0);
  setDfsUsed(0);
  setXceiverCount(0);
  this.blockList=null;
  this.cachedBlockList=null;
  this.invalidateBlocks.clear();
  this.blocksToUncache.clear();
  this.volumeFailures=0;
}
