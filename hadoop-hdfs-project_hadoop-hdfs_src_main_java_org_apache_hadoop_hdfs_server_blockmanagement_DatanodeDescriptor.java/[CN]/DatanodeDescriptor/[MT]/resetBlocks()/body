{
  setCapacity(0);
  setRemaining(0);
  setBlockPoolUsed(0);
  setDfsUsed(0);
  setXceiverCount(0);
  this.invalidateBlocks.clear();
  this.volumeFailures=0;
  this.pendingCached.clear();
  this.cached.clear();
  this.pendingUncached.clear();
}
