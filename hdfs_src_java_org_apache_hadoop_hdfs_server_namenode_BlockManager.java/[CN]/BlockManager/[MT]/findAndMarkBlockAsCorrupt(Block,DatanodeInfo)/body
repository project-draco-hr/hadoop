{
  BlockInfo storedBlock=getStoredBlock(blk);
  if (storedBlock == null) {
    NameNode.stateChangeLog.info("BLOCK* NameSystem.markBlockAsCorrupt: " + "block " + blk + " could not be marked as "+ "corrupt as it does not exist in blocksMap");
    return;
  }
  markBlockAsCorrupt(storedBlock,dn);
}
