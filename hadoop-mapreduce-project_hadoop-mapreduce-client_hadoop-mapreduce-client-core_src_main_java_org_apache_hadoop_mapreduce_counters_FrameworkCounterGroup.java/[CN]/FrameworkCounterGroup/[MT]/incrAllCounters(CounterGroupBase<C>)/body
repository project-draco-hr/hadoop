{
  if (checkNotNull(other,"other counter group") instanceof FrameworkCounterGroup<?,?>) {
    for (    Counter counter : other) {
      findCounter(((FrameworkCounter)counter).key).increment(counter.getValue());
    }
  }
}
