{
  if (!hasRpcAddress()) {
    return false;
  }
  if (!hasHttpAddress()) {
    return false;
  }
  if (!hasStorageInfo()) {
    return false;
  }
  if (!getStorageInfo().isInitialized()) {
    return false;
  }
  return true;
}
