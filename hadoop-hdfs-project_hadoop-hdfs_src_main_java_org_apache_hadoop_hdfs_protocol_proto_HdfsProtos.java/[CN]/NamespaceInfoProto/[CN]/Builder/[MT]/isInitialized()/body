{
  if (!hasBuildVersion()) {
    return false;
  }
  if (!hasDistUpgradeVersion()) {
    return false;
  }
  if (!hasBlockPoolID()) {
    return false;
  }
  if (!hasStorageInfo()) {
    return false;
  }
  if (!getStorageInfo().isInitialized()) {
    return false;
  }
  return true;
}
