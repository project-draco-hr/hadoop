{
  if (!hasAction()) {
    return false;
  }
  if (!hasType()) {
    return false;
  }
  if (hasCheckpointCmd()) {
    if (!getCheckpointCmd().isInitialized()) {
      return false;
    }
  }
  return true;
}
