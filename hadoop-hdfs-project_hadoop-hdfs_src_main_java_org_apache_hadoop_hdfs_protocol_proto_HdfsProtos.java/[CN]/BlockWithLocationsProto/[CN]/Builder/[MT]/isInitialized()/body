{
  if (!hasBlock()) {
    return false;
  }
  if (!getBlock().isInitialized()) {
    return false;
  }
  for (int i=0; i < getDatanodeIDsCount(); i++) {
    if (!getDatanodeIDs(i).isInitialized()) {
      return false;
    }
  }
  return true;
}
