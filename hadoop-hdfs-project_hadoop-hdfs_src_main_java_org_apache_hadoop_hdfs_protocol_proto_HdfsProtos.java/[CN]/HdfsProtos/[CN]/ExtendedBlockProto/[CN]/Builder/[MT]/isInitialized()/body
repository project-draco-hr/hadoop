{
  if (!hasPoolId()) {
    return false;
  }
  if (!hasBlockId()) {
    return false;
  }
  if (!hasNumBytes()) {
    return false;
  }
  if (!hasGenerationStamp()) {
    return false;
  }
  return true;
}
