{
  SSHRemoteExecution sshRE=new SSHRemoteExecution();
  String ret=sshRE.getHomeDir();
  assertEquals(System.getProperty("user.home"),ret + System.getProperty("file.separator") + System.getProperty("user.name"));
}
