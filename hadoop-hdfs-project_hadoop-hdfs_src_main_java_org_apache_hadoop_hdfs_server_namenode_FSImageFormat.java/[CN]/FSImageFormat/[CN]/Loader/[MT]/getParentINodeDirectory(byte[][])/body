{
  if (pathComponents.length < 2) {
    return null;
  }
  final INodesInPath inodes=namesystem.dir.rootDir.getExistingPathINodes(pathComponents,2,false);
  return INodeDirectory.valueOf(inodes.getINode(0),pathComponents);
}
