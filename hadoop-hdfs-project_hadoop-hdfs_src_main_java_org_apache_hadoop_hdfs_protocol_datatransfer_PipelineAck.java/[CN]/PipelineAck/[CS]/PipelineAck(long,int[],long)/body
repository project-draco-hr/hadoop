{
  ArrayList<Integer> replyList=Lists.newArrayList();
  for (  int r : replies) {
    replyList.add(r);
  }
  proto=PipelineAckProto.newBuilder().setSeqno(seqno).addAllReply(replyList).setDownstreamAckTimeNanos(downstreamAckTimeNanos).build();
}
