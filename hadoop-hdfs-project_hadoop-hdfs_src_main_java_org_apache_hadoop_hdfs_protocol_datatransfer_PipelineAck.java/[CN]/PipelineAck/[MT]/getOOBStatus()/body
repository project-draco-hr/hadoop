{
  if (getSeqno() != UNKOWN_SEQNO) {
    return null;
  }
  for (  Status s : proto.getReplyList()) {
    if (s.getNumber() >= OOB_START && s.getNumber() <= OOB_END) {
      return s;
    }
  }
  return null;
}
