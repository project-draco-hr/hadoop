{
  try {
    Key key=new Key(blockId,"test_bp1");
    return new ShortCircuitReplicaInfo(new ShortCircuitReplica(key,pair.getFileInputStreams()[0],pair.getFileInputStreams()[1],cache,Time.monotonicNow()));
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
