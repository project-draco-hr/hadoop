{
  Collection<JobInProgress> queue=waiting ? scheduler.jobQueuesManager.getJobQueue("default").getWaitingJobs() : scheduler.jobQueuesManager.getJobQueue("default").getRunningJobs();
  return queue.toArray(new JobInProgress[0]);
}
