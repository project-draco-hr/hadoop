{
  return Separator.QUALIFIERS.join(Bytes.toBytes(Separator.QUALIFIERS.encode(clusterId)),Bytes.toBytes(TimelineWriterUtils.invert(dayTs)),Bytes.toBytes(Separator.QUALIFIERS.encode(userId)),Bytes.toBytes(Separator.QUALIFIERS.encode(flowId)));
}
