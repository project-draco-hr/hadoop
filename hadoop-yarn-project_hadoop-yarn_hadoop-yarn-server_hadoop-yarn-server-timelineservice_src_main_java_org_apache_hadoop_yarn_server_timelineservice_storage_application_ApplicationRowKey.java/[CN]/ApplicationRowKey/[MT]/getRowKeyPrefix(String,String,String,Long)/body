{
  byte[] first=Bytes.toBytes(Separator.QUALIFIERS.joinEncoded(clusterId,userId,flowId));
  byte[] second=Bytes.toBytes(TimelineWriterUtils.invert(flowRunId));
  return Separator.QUALIFIERS.join(first,second,new byte[0]);
}
