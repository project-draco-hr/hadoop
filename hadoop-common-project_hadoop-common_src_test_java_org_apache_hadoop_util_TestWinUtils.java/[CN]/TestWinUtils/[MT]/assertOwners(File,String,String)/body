{
  String[] args=lsF(file).trim().split("[\\|]");
  assertEquals(expectedUser.toLowerCase(Locale.ENGLISH),args[2].toLowerCase(Locale.ENGLISH));
  assertEquals(expectedGroup.toLowerCase(Locale.ENGLISH),args[3].toLowerCase(Locale.ENGLISH));
}
