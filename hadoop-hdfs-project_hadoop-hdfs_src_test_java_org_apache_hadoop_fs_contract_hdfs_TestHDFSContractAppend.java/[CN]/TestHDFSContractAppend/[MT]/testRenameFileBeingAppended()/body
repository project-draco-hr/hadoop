{
  try {
    super.testRenameFileBeingAppended();
    fail("Expected a FileNotFoundException");
  }
 catch (  FileNotFoundException e) {
    ContractTestUtils.downgrade("Renaming an open file" + "still creates the old path",e);
  }
}
