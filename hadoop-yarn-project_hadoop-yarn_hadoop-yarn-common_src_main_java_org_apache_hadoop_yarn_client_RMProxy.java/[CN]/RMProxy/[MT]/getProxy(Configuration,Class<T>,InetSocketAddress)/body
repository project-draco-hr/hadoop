{
  return (T)UserGroupInformation.getCurrentUser().doAs(new PrivilegedAction<Object>(){
    @Override public T run(){
      return (T)YarnRPC.create(conf).getProxy(protocol,rmAddress,conf);
    }
  }
);
}
