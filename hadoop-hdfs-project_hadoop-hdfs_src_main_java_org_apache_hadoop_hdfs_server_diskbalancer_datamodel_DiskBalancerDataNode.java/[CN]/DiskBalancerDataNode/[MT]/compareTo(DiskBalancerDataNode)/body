{
  Preconditions.checkNotNull(that);
  if (Float.compare(this.nodeDataDensity - that.getNodeDataDensity(),0) < 0) {
    return -1;
  }
  if (Float.compare(this.nodeDataDensity - that.getNodeDataDensity(),0) == 0) {
    return 0;
  }
  if (Float.compare(this.nodeDataDensity - that.getNodeDataDensity(),0) > 0) {
    return 1;
  }
  return 0;
}
