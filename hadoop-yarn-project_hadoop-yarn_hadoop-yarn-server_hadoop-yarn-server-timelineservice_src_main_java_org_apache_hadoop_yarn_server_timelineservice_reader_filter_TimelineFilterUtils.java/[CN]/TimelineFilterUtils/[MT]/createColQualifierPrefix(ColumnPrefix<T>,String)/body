{
  if (colPrefix == ApplicationColumnPrefix.EVENT || colPrefix == EntityColumnPrefix.EVENT) {
    return EventColumnNameConverter.getInstance().encode(new EventColumnName(column,null,null));
  }
 else {
    return StringKeyConverter.getInstance().encode(column);
  }
}
