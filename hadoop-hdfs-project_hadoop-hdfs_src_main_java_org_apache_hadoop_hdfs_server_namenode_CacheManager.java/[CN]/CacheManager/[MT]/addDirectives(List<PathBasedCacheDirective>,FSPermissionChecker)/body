{
  ArrayList<Fallible<PathBasedCacheEntry>> results=new ArrayList<Fallible<PathBasedCacheEntry>>(directives.size());
  for (  PathBasedCacheDirective directive : directives) {
    results.add(addDirective(directive,pc));
  }
  return results;
}
