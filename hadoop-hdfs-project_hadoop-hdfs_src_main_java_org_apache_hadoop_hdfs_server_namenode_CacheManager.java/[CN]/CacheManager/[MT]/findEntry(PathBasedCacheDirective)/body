{
  List<PathBasedCacheEntry> existing=entriesByPath.get(directive.getPath());
  if (existing == null) {
    return null;
  }
  for (  PathBasedCacheEntry entry : existing) {
    if (entry.getPool().getName().equals(directive.getPool())) {
      return entry;
    }
  }
  return null;
}
