{
  String jobIdentifier=(jobId == null) ? "none" : jobId.getAppId().getClusterTimestamp() + "_" + jobidFormat.format(jobId.getAppId().getId())+ "_"+ ((getTaskType() == TaskType.MAP) ? "m" : "r")+ "_"+ idFormat.format(getId());
  return "task_" + jobIdentifier;
}
