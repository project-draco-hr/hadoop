{
  final Path absF=fixRelativePart(f);
  return new FSLinkResolver<Iterator<LocatedFileStatus>>(){
    public Iterator<LocatedFileStatus> next(    final AbstractFileSystem fs,    final Path p) throws IOException, UnresolvedLinkException {
      return fs.listLocatedStatus(p);
    }
  }
.resolve(this,absF);
}
