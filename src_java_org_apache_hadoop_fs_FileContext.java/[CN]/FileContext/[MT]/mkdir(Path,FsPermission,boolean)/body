{
  Path absDir=fixRelativePart(dir);
  FsPermission absFerms=(permission == null ? FsPermission.getDefault() : permission).applyUMask(umask);
  getFSofPath(absDir).mkdir(absDir,absFerms,createParent);
}
