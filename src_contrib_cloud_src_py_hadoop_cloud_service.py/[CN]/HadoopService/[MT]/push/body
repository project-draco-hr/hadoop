def push(self, ssh_options, file):
    master = self._get_master()
    if (not master):
        sys.exit(1)
    subprocess.call(('scp %s -r %s root@%s:' % (xstr(ssh_options), file, master.public_ip)), shell=True)
