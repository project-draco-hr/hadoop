def execute(self, ssh_options, args):
    master = self._get_master()
    if (not master):
        sys.exit(1)
    subprocess.call(("ssh %s root@%s '%s'" % (xstr(ssh_options), master.public_ip, ' '.join(args))), shell=True)
