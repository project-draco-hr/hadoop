{
  Collection<String> nameserviceIds=getNameServiceIds(conf);
  if (nameserviceIds == null || nameserviceIds.isEmpty()) {
    return null;
  }
  for (  String nameserviceId : nameserviceIds) {
    for (    String key : keys) {
      String candidateAddress=conf.get(getNameServiceIdKey(key,nameserviceId));
      if (candidateAddress != null && address.equals(NetUtils.createSocketAddr(candidateAddress)))       return nameserviceId;
    }
  }
  return null;
}
