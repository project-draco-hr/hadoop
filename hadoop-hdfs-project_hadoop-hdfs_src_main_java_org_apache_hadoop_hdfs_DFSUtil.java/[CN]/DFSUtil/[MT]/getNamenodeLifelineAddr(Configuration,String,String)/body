{
  if (nsId == null) {
    nsId=getOnlyNameServiceIdOrNull(conf);
  }
  String lifelineAddrKey=DFSUtilClient.concatSuffixes(DFSConfigKeys.DFS_NAMENODE_LIFELINE_RPC_ADDRESS_KEY,nsId,nnId);
  return conf.get(lifelineAddrKey);
}
