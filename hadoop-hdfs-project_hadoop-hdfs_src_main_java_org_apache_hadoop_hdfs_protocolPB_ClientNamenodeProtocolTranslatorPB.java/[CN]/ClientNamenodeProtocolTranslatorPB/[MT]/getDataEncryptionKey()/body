{
  GetDataEncryptionKeyRequestProto req=GetDataEncryptionKeyRequestProto.newBuilder().build();
  try {
    GetDataEncryptionKeyResponseProto rsp=rpcProxy.getDataEncryptionKey(null,req);
    return rsp.hasDataEncryptionKey() ? PBHelper.convert(rsp.getDataEncryptionKey()) : null;
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
}
