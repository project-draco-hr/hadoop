{
  final DeleteEncryptionZoneRequestProto req=DeleteEncryptionZoneRequestProto.newBuilder().setSrc(src).build();
  try {
    rpcProxy.deleteEncryptionZone(null,req);
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
}
