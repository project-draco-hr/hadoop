{
  ListCorruptFileBlocksRequestProto req=ListCorruptFileBlocksRequestProto.newBuilder().setPath(path).setCookie(cookie).build();
  try {
    return PBHelper.convert(rpcProxy.listCorruptFileBlocks(null,req).getCorrupt());
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
}
