{
  final CreateEncryptionZoneRequestProto req=CreateEncryptionZoneRequestProto.newBuilder().setSrc(src).setKeyId(keyId).build();
  try {
    rpcProxy.createEncryptionZone(null,req);
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
}
