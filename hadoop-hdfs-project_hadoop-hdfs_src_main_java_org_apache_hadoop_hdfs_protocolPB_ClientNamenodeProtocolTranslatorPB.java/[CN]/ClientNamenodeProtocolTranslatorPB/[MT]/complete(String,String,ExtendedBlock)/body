{
  CompleteRequestProto req=CompleteRequestProto.newBuilder().setSrc(src).setClientName(clientName).setLast(PBHelper.convert(last)).build();
  try {
    return rpcProxy.complete(null,req).getResult();
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
}
