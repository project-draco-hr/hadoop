{
  GetErasureCodingZoneInfoRequestProto req=GetErasureCodingZoneInfoRequestProto.newBuilder().setSrc(src).build();
  try {
    GetErasureCodingZoneInfoResponseProto response=rpcProxy.getErasureCodingZoneInfo(null,req);
    if (response.hasECZoneInfo()) {
      return PBHelper.convertECZoneInfo(response.getECZoneInfo());
    }
    return null;
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
}
