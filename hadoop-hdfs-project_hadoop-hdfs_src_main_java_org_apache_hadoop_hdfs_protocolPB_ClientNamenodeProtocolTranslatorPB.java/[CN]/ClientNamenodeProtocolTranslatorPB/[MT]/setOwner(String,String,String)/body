{
  SetOwnerRequestProto req=SetOwnerRequestProto.newBuilder().setSrc(src).setUsername(username).setGroupname(groupname).build();
  try {
    rpcProxy.setOwner(null,req);
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
}
