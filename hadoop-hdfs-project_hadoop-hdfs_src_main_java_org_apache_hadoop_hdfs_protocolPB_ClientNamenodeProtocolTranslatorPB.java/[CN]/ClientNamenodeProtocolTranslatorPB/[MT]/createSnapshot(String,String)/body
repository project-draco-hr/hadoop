{
  CreateSnapshotRequestProto req=CreateSnapshotRequestProto.newBuilder().setSnapshotName(snapshotName).setSnapshotRoot(snapshotRoot).build();
  try {
    rpcProxy.createSnapshot(null,req);
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
}
