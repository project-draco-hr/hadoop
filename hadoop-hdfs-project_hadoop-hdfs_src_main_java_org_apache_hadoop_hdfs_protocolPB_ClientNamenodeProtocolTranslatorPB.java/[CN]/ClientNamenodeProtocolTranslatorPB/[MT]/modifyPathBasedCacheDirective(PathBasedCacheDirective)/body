{
  try {
    rpcProxy.modifyPathBasedCacheDirective(null,ModifyPathBasedCacheDirectiveRequestProto.newBuilder().setInfo(PBHelper.convert(directive)).build());
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
}
