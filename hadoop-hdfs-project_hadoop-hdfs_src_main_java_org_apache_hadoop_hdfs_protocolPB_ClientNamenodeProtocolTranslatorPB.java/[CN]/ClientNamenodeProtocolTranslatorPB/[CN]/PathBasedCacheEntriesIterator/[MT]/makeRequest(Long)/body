{
  ListPathBasedCacheDirectivesResponseProto response;
  try {
    response=rpcProxy.listPathBasedCacheDirectives(null,ListPathBasedCacheDirectivesRequestProto.newBuilder().setPrevId(nextKey).setFilter(PBHelper.convert(filter)).build());
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
  return new BatchedPathBasedCacheEntries(response);
}
