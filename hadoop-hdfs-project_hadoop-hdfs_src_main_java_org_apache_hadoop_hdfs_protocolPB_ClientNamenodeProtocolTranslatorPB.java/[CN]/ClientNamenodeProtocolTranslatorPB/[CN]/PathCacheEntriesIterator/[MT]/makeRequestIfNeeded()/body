{
  if (idx == -1) {
    makeRequest();
  }
 else   if ((response != null) && (idx >= response.getElementsCount())) {
    if (response.getHasMore()) {
      makeRequest();
    }
 else {
      response=null;
    }
  }
}
