{
  makeRequestIfNeeded();
  if (response == null) {
    throw new NoSuchElementException();
  }
  ListPathCacheEntriesElementProto elementProto=response.getElements(idx);
  prevId=elementProto.getId();
  idx++;
  return new PathCacheEntry(elementProto.getId(),new PathCacheDirective(elementProto.getPath(),elementProto.getPool()));
}
