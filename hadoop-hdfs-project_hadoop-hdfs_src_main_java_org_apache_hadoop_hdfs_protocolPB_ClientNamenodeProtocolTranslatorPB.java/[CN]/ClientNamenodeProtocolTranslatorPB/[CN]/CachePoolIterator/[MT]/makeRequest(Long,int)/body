{
  try {
    return new BatchedPathDirectiveEntries(rpcProxy.listCachePools(null,ListCachePoolsRequestProto.newBuilder().setPrevPool(PBHelper.convert(new CachePool(prevKey))).setMaxReplies(maxRepliesPerRequest).build()));
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
}
