{
  try {
    return new BatchedPathDirectiveEntries(rpcProxy.listCachePools(null,ListCachePoolsRequestProto.newBuilder().setPrevPoolName(prevKey).setMaxReplies(maxRepliesPerRequest).build()));
  }
 catch (  ServiceException e) {
    throw ProtobufHelper.getRemoteException(e);
  }
}
