{
  ListPathBasedCacheEntriesElementProto elementProto=response.getElements(i);
  return new PathBasedCacheEntry(elementProto.getId(),new PathBasedCacheDirective(elementProto.getPath(),elementProto.getPool()));
}
