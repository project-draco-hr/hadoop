{
  ListPathBasedCacheDescriptorsElementProto elementProto=response.getElements(i);
  return new PathBasedCacheDescriptor(elementProto.getId(),new Path(elementProto.getPath()),Shorts.checkedCast(elementProto.getReplication()),elementProto.getPool());
}
