{
  namenode.getNamesystem().readLock();
  try {
    return FSDirStatAndListingOp.getFileInfo(namenode.getNamesystem().getFSDirectory(),src,resolveLink);
  }
  finally {
    namenode.getNamesystem().readUnlock();
  }
}
